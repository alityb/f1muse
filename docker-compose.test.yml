services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: f1muse_test
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "127.0.0.1:5433:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d f1muse_test"]
      interval: 2s
      timeout: 2s
      retries: 15
