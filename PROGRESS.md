# Progress

## Autonomous Roadmap Progress

### Phase 0: COMPLETE
- Translation entry: `src/api/routes/program-translate.ts` is feature-gated, shadow-only, and does not call `executeF1QL`.
- Translation adapters: `src/f1ql/translator.ts` supports Anthropic and OpenAI-compatible tool calls; production uses Groq.
- Surface schema/lowering: `src/f1ql/ast.ts`, `schema.ts`, `lower.ts`.
- Core/compiler/interpreters: `src/f1ql/core.ts`, `compiler.ts`, `interpreter.ts`, `executor.ts`.
- Existing budgets: `src/f1ql/limits.ts`; API route handles program metrics/logging.
- Tests: `tests/f1ql/*`, `tests/api/in-process-routes.test.ts`; local PostgreSQL is bootstrapped by `src/test/setup.ts`.
- CI: `.github/workflows/test.yml` runs typecheck, lint, unit, golden, integration, schema, and in-process API gates.
- Property testing: no library is installed. `fast-check` is the appropriate TypeScript dependency for Phase 5, but will be introduced only with bounded generators.

### Phase Status
- Phase 1: COMPLETE. The Railway historical-log fetch was proven during Phase 2.
- Phase 2: COMPLETE. All six validation gates, local definition-of-done suites, typed observability flow, and the production shadow round-trip are verified.
- Phase 3: COMPLETE. Pace, classification, and standings execute through the generic core IR; no macro-shaped pace execution remains.
- Phase 4: COMPLETE. Areas 1-3 and retirement-reason normalization are complete; the bounded guarded production retirement sample is recorded in `docs/PRODUCTION_EVIDENCE_LEDGER.md`.
- Phase 4 production verification: deployment `b5dd7e4d-da25-444a-ac05-67a0481d3a40` reached `SUCCESS`; a known-driver shadow request produced one logged `pace_summary` attempt with 100.00% success and no rejection reasons. Retirement sampling is complete; see `docs/PRODUCTION_EVIDENCE_LEDGER.md`.
- Phase 5: COMPLETE. The 100-case corpus, bounded `fast-check` core-IR properties, metamorphic and differential database/reference tests, production factual corpus, source contracts, performance observation, lifecycle audit, and nightly production verification are complete. The nullable per-race points boundary and pace factual boundary remain explicit product/source limits, not unresolved Phase 5 work.
- Phase 6: COMPLETE. `4f4e9dc` adds a pure verified-program library, semantic normalized-program SHA-256 hashes, version-bound cache keys, and `executeVerifiedF1QL`. Verified execution delegates to `executeF1QL`, so it reruns parse, validation, cost, participation, lowering, core validation, compilation, row caps, and read-only timeout execution. Shadow translation has no library/cache/execution integration. The reviewed registry migration and production round-trip are recorded in `docs/PRODUCTION_EVIDENCE_LEDGER.md`.
- `20260728_f1ql_verified_program_fact_space.sql` defines only versioned reviewed registries: `f1ql.verified_program` and `f1ql.fact_space_revision`. It has no application writer and was exercised only against the localhost test database. `GET /program/verified` lists curated library metadata; `POST /program/verified/:id` executes only through `executeVerifiedF1QL` and the existing guarded pipeline. The translation route remains unchanged and its throwing-executor invariant continues to pass. Verification passed: `npm run typecheck`, `npm run test:f1ql` (257 tests in 36 files), `npm run test:api:inprocess` (8 tests), and `npm run lint` (0 errors, 123 pre-existing warnings).
- Production completion: migration `20260728_f1ql_verified_program_fact_space.sql` applied successfully through Railway; deployment `dfb17cae-aa29-4db9-ad79-fe0a65f24a6d` reached `SUCCESS`. The deployed `POST /program/verified/2025-driver-standings` returned the canonical 2025 standings; retained response SHA-256 `460373b3e1076c29c3aacd0136b4e8d2c4e162ae22a142a7c3eb1bb2c69912ba`. A deployed `POST /program/translate` shadow request returned `program_unsupported` without execution; retained response SHA-256 `a24d4082694ced572195aeca98c7ba11c427a1ab8307f72a5ac590419545ba78`.

### Phase 7 Roadmap Validation (2026-07-23)
- Four parallel reviews covered repository integration, safe NL analytics architecture, semantic parsing evaluation, and F1-specific ambiguity/source rules. `docs/NL_EXECUTION_AND_HISTORICAL_PACE_ROADMAP.md` now reflects the actual F1QL v1 surface grammar and adds typed clarification/abstention, canonical driver/event linking, capability-tuple policy, deterministic formatting, least-privilege proof, bounded resources, selective semantic evaluation, adversarial tests, and staged canary gates.
- Important launch exclusions are explicit: pace, sprint, grid, constructor, interim standings, unresolved team filters, multi-season/unscoped standings, and arbitrary race/qualifying aggregation or comparison. The shadow translation route remains structurally separate and non-executing.
- Research references include Oracle NL2SQL separation, governed semantic layers from dbt/Snowflake, OWASP prompt-injection controls, PostgreSQL least privilege, Spider/test-suite accuracy/Dr.Spider, PRACTIQ/AMBROSIA ambiguity evaluation, and Google SRE canarying. No application code, production command, migration, deploy, or translated-query execution occurred.

### Phase 7 Work Package 1: COMPLETE (2026-07-23)
- Translation now returns a strict discriminated result: `program_candidate`, `clarification_required`, `unsupported`, or `provider_unavailable`. The model tool contract emits an envelope instead of inventing the schema-invalid `unsupported` F1QL operation; candidate programs are still reparsed by the canonical F1QL schema before crossing the translation boundary.
- Clarification and unsupported outcomes accept only the reviewed stable reason-code allowlists. Provider exceptions, non-JSON output, malformed envelopes, and invalid candidate programs fail closed with distinct typed outcomes and no fallback program.
- The shadow endpoint maps typed results to deterministic HTTP responses and metrics, resolves and validates only `program_candidate`, and remains non-executing. Its injected throwing-executor regression passed with added focused-clarification coverage.
- Verification passed: `npm run typecheck`, `npm run test:f1ql` (259 tests in 36 files), `npm run test:api:inprocess` (8 tests), and `npm run lint` (0 errors, 123 pre-existing warnings). No answer endpoint, execution path, production command, write, DDL, deploy, or translated-query execution was added.
- Exact next step: add a translation-only candidate schema that can carry a season plus named-event reference while keeping canonical F1QL restricted to `{season, round}`; resolve that reference to exactly one database event before canonical parsing. Add an ambiguity-preserving driver-resolution mode for Phase 7 so duplicate literal aliases return candidates rather than using the legacy winner-take-all ranking, then apply it to standings, race, and qualifying driver filters. Cover unique, missing, and ambiguous event/driver cases with localhost fixtures; do not add or enable `/program/answer` execution in this work package.

### Phase 7 Work Package 2: COMPLETE (2026-07-23)
- Added a strict translation-only named-event candidate schema. It accepts a scalar season plus `event_name` only for race classification, qualifying classification, or event metadata; canonical F1QL remains unchanged and retains only `{season, round}`.
- Added a season-bounded literal `EventResolver` over `race`/`grand_prix`, with deterministic unique, missing, and ambiguous outcomes. It matches reviewed database identity/name fields and the explicit `Belgium` to `Belgian Grand Prix` alias; ambiguity options are sorted by season/round and translation-only event names never cross canonical parsing.
- Added `DriverResolver.resolveUnambiguous` without changing legacy resolver behavior. It preserves duplicate same-season literal candidates, uses exact season participation to remove historical namesakes, and never invokes teammate, coverage, or lexical winner-take-all ranking. Shadow linking now covers pace, standings aggregate/rank, race classification, and qualifying classification driver locations.
- The linked output is reparsed by the canonical F1QL schema before validation. This rejects post-link invariant failures such as two aliases collapsing to the same driver in `pace_delta`. Missing events abstain with `source_coverage_missing`; event/driver ambiguity returns focused clarification with deterministic options.
- Independent review findings were addressed for canonical post-link reparsing and the documented `Belgium 2021` form. Unresolved team filters remain intentionally outside this linking package and must be rejected by the answer-specific capability policy before any execution eligibility.
- Verification passed: `npm run typecheck`, `npm run test:f1ql` (263 tests in 36 files), `npm run test:api:inprocess` (8 tests), and `npm run lint` (0 errors, 123 pre-existing warnings). The shadow throwing-executor invariant remains zero. No answer endpoint, execution path, production command, write, DDL, deploy, or translated-query execution was added.
- Exact next step: implement a pure answer-specific capability-tuple policy over fully linked canonical F1QL. Allow only one-season final driver standings, race classification, qualifying classification, and race-date event metadata; reject pace, team filters, multi-season/unscoped standings, unsupported classification composition, and all other excluded tuples with stable reason codes. Add table-driven allow/deny tests before creating an independently gated, still-disabled `/program/answer` skeleton.

### Phase 7 Work Package 3: COMPLETE (2026-07-23)
- Added a pure answer-specific authorization policy over fully linked canonical F1QL. Approved decisions carry a semantic capability tuple with source, operation, scalar season, optional round, and the exact allowed filter class; denied decisions carry a stable low-cardinality reason.
- The initial allowlist is final driver standings for one season, race classification, qualifying classification, and race-date event metadata. Standings driver sets are capped at four. Classification permits either one resolved driver or one classification status, never both; team filters and qualifying-session metadata are denied.
- Final standings are explicitly versioned through 2025. Unscoped, multi-season, ongoing 2026, and future standings are denied rather than mislabeled final. Pace is denied, and an explicit default rejects runtime-unknown operations even though callers must also canonical-parse first.
- Independent review found and drove closure of the interim-standings, classification-composition, and unknown-operation fail-open gaps. Table-driven coverage now has six approved and thirteen denied capability cases, including both team-filter sources and malformed runtime operation input.
- Verification passed: `npm run typecheck`, `npm run test:f1ql` (282 tests in 37 files), and `npm run lint` (0 errors, 123 pre-existing warnings). The policy is not called by the shadow route and no answer endpoint, execution path, production command, write, DDL, deploy, or translated-query execution was added.
- Exact next step: add an independent `/program/answer` router skeleton with `F1QL_ANSWER_ENABLED` and emergency kill-switch checks that occur before model construction or translation. Inject translation/linking/policy/execution dependencies so tests can prove disabled, killed, clarification, unsupported, and policy-denied requests invoke the executor zero times. Keep successful policy-approved requests non-executing until answer-specific concurrency, timeout, and response bounds are implemented.

### Phase 7 Non-Executing Answer Skeleton: COMPLETE (2026-07-23)
- Added an independently mounted `POST /program/answer` skeleton. `F1QL_ANSWER_KILL_SWITCH` and `F1QL_ANSWER_ENABLED` request guards run before question validation, model construction, translation, linking, or policy; API discovery advertises the route only while enabled and not killed.
- Extracted deterministic event/driver linking into shared `translation-linking.ts`; the shadow route still owns its existing validation/participation flow and remains structurally separate. The answer route lazily constructs its model only after both operational guards and validates question length before construction.
- Translation clarification/abstention, provider failure, linking ambiguity/missing coverage, policy denial, and unexpected provider/linker/authorization failures have distinct deterministic responses. Approved capabilities return `execution_bounds_not_configured` in `gated_non_execution` mode.
- The answer route has no executor import, execution dependency, or execution call. A source-level invariant enforces that boundary, while route tests cover disabled, killed, invalid input, clarification, abstention, malformed provider output, linking ambiguity, policy denial, and approved-but-blocked requests. The existing shadow throwing-executor invariant remains green.
- Independent review drove operational-error classification, conditional API discovery, removal of the misleading unused executor seam, and broader pre-execution branch coverage.
- Verification passed: `npm run typecheck`, `npm run test:f1ql` (292 tests in 38 files), `npm run test:api:inprocess` (9 tests), and `npm run lint` (0 errors, 123 pre-existing warnings). No translated program executed; no production command, write, DDL, or deploy occurred.
- Exact next step: implement answer-specific validated configuration and bounded model/database concurrency with request-local cancellation, while retaining the no-executor-import invariant. Add answer-specific rate limiting at route registration and pre-execution work/row/response-byte budget types; only after these gates are green may a separately reviewed milestone inject `executeF1QL` for exactly one approved execution.

### Phase 7 Answer Runtime Admission: COMPLETE (2026-07-23)
- Added strict answer-only runtime configuration with conservative defaults and fail-closed ceilings for concurrency, queue wait, request and statement deadlines, IP rate limiting, work units, rows, and response bytes. The answer request maximum is below the existing global 15-second API deadline, statement timeout cannot exceed request timeout, and rate windows cannot be configured below one minute.
- Added an independent cancellable admission controller. It bounds active answer translation/linking work, removes disconnected queued requests, returns a stable busy response at the queue deadline, and releases permits exactly once. It does not share or weaken the legacy NL limiter.
- The answer route now has a dedicated IP limiter and request-local abort controller. Abort propagates into both Anthropic and OpenAI-compatible translation calls; disconnect and timeout state takes precedence over translation/linking errors. Default identity linking obtains one bounded client and runs in a read-only transaction with a local PostgreSQL statement timeout; a timed-out pool acquisition releases any client that arrives late.
- Configured work/row/response-byte values are types and validated boundaries only, not execution claims. Approved programs remain non-executing and return `execution_bounds_not_enforced`; the route still has no executor import, dependency, or call.
- Independent review identified unbounded linker occupancy, timeout/error races, the global-timeout conflict, an ineffective minimum rate window, and lint failures. These were resolved before commit. Verification passed: `npm run typecheck`; `npm run lint` with 0 errors and 126 warnings; `npm run test:f1ql` with 303 tests in 39 files; and `npm run test:api:inprocess` with 9 tests. No translated program executed; no production command, write, DDL, or deploy occurred.
- Exact next step: enforce deterministic pre-execution work-unit estimates and post-execution row/serialized-byte caps, prove an answer-specific least-privilege database role and telemetry, and add source-specific deterministic formatting. Keep the executor structurally absent until those gates and adversarial/evaluation suites are reviewed and green.

### Phase 7 Answer Bounds and Formatting: COMPLETE (2026-07-23)
- Added versioned `answer-work-v1` estimation for every approved capability tuple. The estimator independently re-authorizes and exactly matches the capability envelope, conservatively charges unfiltered standings against the executor's full 100-row collection ceiling, and enforces both work-unit and requested-row budgets before an approved answer request reaches the still-blocked execution boundary.
- Added executor-level configurable row collection bounds. Every existing F1QL execution now applies a parameterized `maxRows + 1` outer sentinel and an explicit source-specific top-level order; overflow raises `F1QLResultLimitError`. The default remains the prior 100-row limit, custom values cannot weaken that ceiling, and a Docker-backed exact/overflow test proves deterministic ranked order.
- Added exact UTF-8 response serialization bounds and a deterministic answer envelope/formatter for final standings, race classification, qualifying classification, and race-date metadata. It carries program hashes and active definitions/compiler/fact-space versions, preserves raw rows and nulls, treats empty rows as unavailable rather than zero, identifies possible classification truncation, and canonicalizes decimal strings without floating-point precision loss.
- Independent review found numeric precision/malformed-field acceptance, stale capability coupling, estimated-row non-enforcement, invalid numeric maxima, and loss of formal top-level SQL order. All were resolved before commit. The answer route retains its source-level no-executor invariant and now has a covered work-budget rejection branch.
- Verification passed: `npm run typecheck`; `npm run lint` with 0 errors and 127 warnings; sequential `npm run test:f1ql` with 320 tests in 41 files; and `npm run test:api:inprocess` with 9 tests. An initial parallel invocation of the two wrapped Docker suites collided on their shared named container before tests ran; the required sequential rerun was fully green. No translated program executed; no production command, write, DDL, or deploy occurred.
- Exact next step: add low-cardinality answer-stage/bound/capability telemetry without question, entity, program, SQL, parameter, or row labels. Then implement a read-only least-privilege audit contract and local overprivilege-detection fixture; production least-privilege completion remains PARTIAL until an explicitly authorized grant migration and read-only audit using the actual answer principal are retained.

### Phase 7 Telemetry and Principal Audit: PARTIAL (2026-07-23)
- Added `f1muse_f1ql_answer_outcomes_total` and matching JSON metrics for fixed pipeline stages, outcomes, and stable reason codes. Gate, input, translation, linking, policy, bound, and structurally blocked execution branches emit no question text, identities, programs, SQL, parameters, or rows. The work-bound route regression proves the exact low-cardinality metric key.
- Added a dual-confirmation, non-loopback `audit:answer-principal:production` reader over `F1QL_ANSWER_DATABASE_URL`. It uses one read-only transaction and local statement timeout, checks dangerous role/database/schema attributes, requires SELECT-only access to the four answer sources plus three dedicated identity/participation views, rejects write privileges, and rejects direct SELECT on the five public identity/event tables.
- Local mocked catalog fixtures prove both exact-pass and overprivileged/missing-view outcomes without DDL or credentials. The current application still links through public base tables, while the required `f1ql.answer_driver_identity`, `f1ql.answer_event_identity`, and `f1ql.answer_season_participation` views do not exist; therefore no production principal can yet satisfy both the application and this least-privilege contract.
- Verification passed: `npm run typecheck`; `npm run lint` with 0 errors and 127 warnings; `npm run test:f1ql` with 323 tests in 42 files; and `npm run test:api:inprocess` with 9 tests. No audit was run against production, no translated program executed, and no production command, write, DDL, grant, deploy, or secret output occurred.
- PARTIAL finishing instructions: create and review a migration for those three narrow views and a dedicated `f1ql_answer` role; update answer-only linking/participation to use the views; explicitly authorize and apply the migration; set a separate `F1QL_ANSWER_DATABASE_URL`; run `F1QL_ANSWER_PRINCIPAL_AUDIT_ENABLED=true F1QL_ANSWER_PRINCIPAL_AUDIT_TARGET=production npm run audit:answer-principal:production`; retain the sanitized report hash and deployed commit in the production evidence ledger. Do not grant the answer role SELECT on public base tables or execute translated programs during this proof.
- Exact next step: implement the three answer identity/participation view contracts and route answer-only linking through them using localhost fixtures. Keep role/grant DDL separate and unapplied, retain the no-executor-import invariant, then add the adversarial/selective semantic evaluation corpus before any execution wiring.

### Phase 7 Least-Privilege Identity Boundary: COMPLETE LOCALLY (2026-07-23)
- Added the unapplied `20260729_f1ql_answer_identity_views.sql` migration with three security-barrier, PUBLIC-revoked views. Event and driver identities expose only flattened identity values; participation exposes only season, driver, and explicit entrant-versus-legacy provenance so candidate-set fallback behavior remains exact. Role creation and grants are intentionally absent.
- Added distinct answer event/driver resolvers and `linkAnswerF1QLCandidate`. The answer route now reads only `f1ql.answer_event_identity`, `f1ql.answer_driver_identity`, and `f1ql.answer_season_participation`; shadow translation remains on the legacy base-table resolver. Normalization, ambiguity, canonical reparsing, and entrant-first fallback semantics remain application-owned.
- Added a dedicated read-only answer pool sourced only from `F1QL_ANSWER_DATABASE_URL`. Route construction no longer falls back to the replica pool; an enabled request without that pool fails before model construction with `answer_database_not_configured`. The principal audit now inventories every effective non-system relation privilege and rejects SELECT outside the exact seven-view allowlist.
- Localhost migration tests prove idempotency, exact columns, PUBLIC revocation, primary-participation precedence, and successful answer linking under a restricted role that cannot SELECT `driver`. The migration was not applied outside the ephemeral wrapped test database.
- Verification passed: `npm run typecheck`; `npm run lint` with 0 errors and 127 warnings; sequential `npm run test:f1ql` with 327 tests in 43 files; and `npm run test:api:inprocess` with 10 tests. The answer route remains executor-free. No translated program executed; no production command, write, migration, grant, deploy, or secret output occurred.
- PARTIAL production boundary remains: explicitly authorize/apply the view migration and separate role/grant DDL, configure the dedicated credential, run the read-only principal audit, and retain evidence. Exact next step: add the reviewed selective/adversarial evaluation manifest and deterministic runner covering answer/clarify/abstain, aliases, ambiguity, prompt injection, capability escalation, null/tie/empty formatting, and oversized bounds before considering execution wiring.

### Phase 7 Selective Evaluation Scaffold: PARTIAL (2026-07-23)
- Added 22 reviewed cases split across development, IID holdout, temporal/entity holdout, and adversarial sets. The annotations cover allowed capabilities, ambiguity, prompt injection, capability escalation, aliases, collision, oversized requests, unsupported sources, and null/tie/empty-result risks.
- Added an offline scorer that accepts observations independently from reviewed expectations. It reports explicit action, reason, and normalized-program denominators plus unsafe answers, false abstentions, false clarifications, and per-split action counts. An answer with the right action but the wrong normalized program is unsafe.
- Added a deterministic policy-selection adapter and source-level regression proving this evaluation module has no executor or database dependency. The tests intentionally synthesize observations only to verify scorer arithmetic; they do not claim translator, provider, fixture-execution, or semantic accuracy.
- Expected null/tie/empty rows are reviewed annotations only and were not emitted by a real executor/formatter. They must not be promoted to emitted-output fixtures or counted as execution evidence until generated through the real bounded emitter from a distinguishing localhost database.
- Verification passed: `npm run typecheck`; `npm run lint` with 0 errors and 128 warnings; and wrapped `npm run test:f1ql` with 332 tests in 44 files. No translated program executed; no production command, write, DDL, grant, deploy, provider evaluation, or secret output occurred.
- PARTIAL finishing instructions: add component-level AST scoring, candidate-recall and complete-link scoring, risk/worst-group metrics, and metamorphic cases; generate distinguishing expected fixtures through the real bounded emitter; then collect independent live-provider observations without executing translated programs and retain per-source/per-operation holdout evidence. Production least-privilege proof remains separately blocked on explicit migration/grant authorization.
- Exact next step: add component-level normalized-program scoring and linker/risk coverage metrics to the independent-observation runner, with focused arithmetic tests and no translated execution.

### Phase 7 Selective Evaluation Metrics: COMPLETE LOCALLY (2026-07-23)
- Added independent component counts for source, scope, entities, filters, operation, ordering, and limits. Each observation is scored against one coherent best-matching acceptable program, so components from mutually exclusive interpretations cannot combine into a false perfect score.
- Added explicit observation completeness, candidate-entity recall, alternative-aware complete-link accuracy, per-risk unsafe/selection counts, and deterministic worst-risk selection accuracy. Missing observations are not classified as model abstentions, and duplicate case IDs plus duplicate/unknown observation IDs fail closed.
- Tightened reviewed entity annotations for named tie/empty/null cases and local ambiguous driver/event candidates. Synthetic perfect observations remain scorer-arithmetic tests only; they are not provider, linker, semantic, or production evidence.
- Independent review identified and drove closure of denominator omissions, incoherent alternative-program scoring, unioned alternative links, missing-observation inflation, action-only risk accuracy, and malformed observation-set acceptance.
- Verification passed: `npm run typecheck`; `npm run lint` with 0 errors and 129 warnings; and wrapped `npm run test:f1ql` with 335 tests in 44 files. No translated program executed; no production command, write, DDL, grant, deploy, provider evaluation, or secret output occurred.
- PARTIAL evaluation boundary remains: distinguishing fixture results must be generated through the real bounded emitter, metamorphic answer-selection cases are not yet represented, and no independent live-provider observations exist. Exact next step: add reviewed paraphrase/alias/filter-order metamorphic groups and a deterministic consistency scorer without executing translated programs.

### Phase 7 Metamorphic Selection Evaluation: COMPLETE LOCALLY (2026-07-23)
- Expanded the reviewed manifest to 25 cases and added three explicit equivalent-question groups for paraphrase, driver/event aliases, and standings driver-filter reordering. The championship-leader pair uses a descending points rank with limit 1 rather than an under-specified all-driver aggregate.
- Added a separate deterministic consistency scorer over independently supplied observations. A group is complete only when every member has a valid action, non-empty reason, and exactly the required program presence; consistency requires the same action, stable reason, and normalized program across the whole group.
- Missing, malformed, program-divergent, duplicate-observation, duplicate-group, and invalid-group shapes cannot receive consistency credit. Filter-order equivalence relies only on the existing canonical F1QL normalization.
- Independent review found and drove closure of the incorrect leader gold program and malformed-observation inflation. Re-review reported no remaining findings and confirmed no executor or database dependency was introduced.
- Verification passed: `npm run typecheck`; `npm run lint` with 0 errors and 129 warnings; and wrapped `npm run test:f1ql` with 338 tests in 44 files. No translated program executed; no production command, write, DDL, grant, deploy, provider evaluation, or secret output occurred.
- PARTIAL evaluation boundary remains: expected distinguishing outputs have not been generated through the real bounded executor/formatter, and no independent live-provider observations exist. Exact next step: design a localhost-only distinguishing fixture emitter for reviewed canonical programs, generate outputs through the real bounded path rather than by hand, and keep translated observations structurally disconnected from execution.

### Phase 7 Distinguishing Answer Fixtures: COMPLETE LOCALLY (2026-07-23)
- Added a dedicated disposable answer-evaluation database seed and `golden:snapshot:answer-evaluation` wrapper. The wrapper hardcodes the localhost test database lifecycle and emits fixtures only for reviewed canonical programs attached to answerable manifest cases.
- Removed hand-authored expected rows. The committed 12-case JSON fixture was generated by authorization, answer work/row enforcement, the real read-only `executeF1QL` path with statement and collection limits, deterministic `buildAnswerEnvelope`, and response-byte serialization.
- The distinguishing seed covers a null finishing position with DNF reason, equal standings points with deterministic driver ordering, an absent round that formats as empty rather than zero, ranked championship-leader semantics, race and qualifying classification, metadata, and reordered driver filters.
- A Docker-backed regression rebuilds the disposable database, reruns the same bounded canonical-program emitter, and compares the full generated envelopes including rows, normalized numeric display, program hashes, rendering, source/version metadata, coverage, and caveats. The emitter imports no translator or observation artifact and cannot execute translated candidates.
- Independent review reported no findings, confirmed all 12 reviewed answer cases are emitted, and verified the localhost and canonical-program boundaries. Verification passed: `npm run typecheck`; `npm run lint` with 0 errors and 129 warnings; and wrapped `npm run test:f1ql` with 339 tests in 45 files.
- Local reviewed canonical programs executed only against the disposable localhost fixture database. No translated program, provider observation, production command, production read/write, DDL, grant, deploy, or secret output occurred.
- PARTIAL evaluation boundary remains: no independent live-provider observations or per-source/per-operation holdout thresholds are retained. Exact next step: add a strict observation-artifact schema and non-executing collection command that records provider translation/linking/policy outcomes separately from reviewed expectations, then run it only if an existing provider credential is available without exposing it.

### Phase 7 Provider Observation Artifact: COMPLETE LOCALLY / PARTIAL LIVE (2026-07-23)
- Added a strict versioned observation artifact that binds all 25 reviewed cases and expectations through a full-manifest SHA-256 plus exact case IDs. Its discriminated schema rejects invalid action/reason/program combinations, duplicate IDs or entities, non-canonical and unsorted entity identifiers, linked entities outside resolver candidates, and answer links inconsistent with the normalized canonical program.
- Added independently observed answer-link candidate sets. Successful and ambiguous event/driver resolution records actual resolver candidates rather than copying post-link entities; database failures remain `linking_unavailable`, canonical parse failures are `program_invalid`, and only typed bound failures become bound reasons. The distinguishing fixture now represents the reviewed rounds 8/9 event ambiguity and duplicate Alex Smith identities.
- Added `collect:answer-evaluation:local`, guarded by two explicit evaluation flags and exact equality with the disposable Docker database URI before any destructive setup. Pool acquisition and all setup statements are bounded, linking uses a read-only transaction with a 1000 ms local timeout, each provider call has a 15-second abort, OpenAI-compatible generation is capped at 512 tokens and 65536 response bytes, and the mode-0600 artifact is written only under an OS-temporary directory. Stdout contains only artifact path/hash/count and provider/model metadata.
- The collector imports no executor or formatter and cannot execute translated programs. Reviewed expectations, generated canonical-program result fixtures, and provider observations remain separate. Database-backed regressions prove unique candidates plus ambiguous event and driver candidates; source-level invariants prove the collector and artifact module have no executor dependency.
- Independent review found and drove closure of the direct localhost-database reset risk, fake post-link candidate recall, absent ambiguous-event fixture, invalid artifact semantics, inaccurate failure reasons, unbounded OpenAI response/setup work, PostgreSQL URI-parameter bypass, contradictory entity observations, database-error misclassification, and underscore/hyphen candidate mismatch. Final re-review reported no findings.
- Verification passed: `npm run typecheck`; `npm run lint` with 0 errors and 130 warnings; `git diff --check`; and wrapped `npm run test:f1ql` with 345 tests in 46 files. No live-provider collection, translated execution, production command, production read/write, DDL, grant, deploy, or secret output occurred.
- PARTIAL live boundary: no provider credential was established for this environment, so no independent provider artifact or accuracy claim exists. Finishing instructions: configure one existing evaluation provider without printing its credential, run `F1QL_ANSWER_EVALUATION_ENABLED=true F1QL_ANSWER_EVALUATION_TARGET=localhost npm run collect:answer-evaluation:local`, validate the resulting artifact against the committed manifest, score selection/link/risk/metamorphic metrics, retain only sanitized hashes and aggregate evidence, and do not execute any translated program.
- Exact next step: add a non-executing report command that validates a collected artifact and emits sanitized aggregate selection, linker, risk, and metamorphic metrics with per-source/per-operation holdout thresholds; use a live artifact only if a credential is safely available.

### Phase 7 Sanitized Provider Evaluation Report: COMPLETE LOCALLY (2026-07-23)
- Added `report:answer-evaluation`, which accepts exactly one strict observation artifact, validates it against the complete reviewed manifest, and emits only aggregate selection, reason, component, linker, risk, metamorphic, source-threshold, operation-threshold, and release-gate counts. It omits questions, case IDs, programs, entities, paths, raw observations, model names, and model-name fingerprints.
- The report applies a conservative 100% action/reason/normalized-program requirement independently to every represented non-development source and operation. Missing evidence is `insufficient`, not pass; semantic or safety errors take precedence and report `fail`. Zero unsafe/forbidden answers, complete observations, exact reasons, candidate recall, complete links, and complete metamorphic consistency are separate gates.
- The strict artifact now additionally requires each answer reason to equal its re-authorized capability source. Input is capped at 1,000,000 bytes through one nonblocking opened descriptor, only regular files are accepted, allocation is fixed from `fstat`, incomplete or growing reads refuse, malformed inputs emit one fixed sanitized refusal, and neither the scorer nor command imports an executor.
- The current reviewed holdout covers all four allowed sources and `aggregate`, `event_classification`, `qualifying_classification`, and `event_metadata`, but not `rank`; therefore even perfect synthetic scorer inputs correctly report the operation gate as `insufficient`. Synthetic observations test report arithmetic only and are not provider evidence.
- Independent review found and drove closure of missing reason gates, enumerable model-name hashing, stat/read replacement risk, and FIFO blocking. Final re-review reported no findings.
- Verification passed: `npm run typecheck`; `npm run lint` with 0 errors and 130 warnings; `git diff --check`; and wrapped `npm run test:f1ql` with 349 tests in 47 files. No live-provider collection, translated execution, production command, production read/write, DDL, grant, deploy, or secret output occurred.
- PARTIAL evaluation boundary remains: no independent live-provider artifact exists, and `rank` lacks non-development semantic evidence. Exact next step: add one reviewed IID or temporal holdout championship-leader case using the existing rank program, regenerate its expected answer envelope through `golden:snapshot:answer-evaluation`, and prove every required source/operation threshold has a nonzero denominator without claiming provider accuracy.

### Phase 7 Complete Local Semantic Threshold Coverage: COMPLETE LOCALLY (2026-07-23)
- Expanded the reviewed manifest to 26 cases with one IID holdout championship-leader question. All four allowed sources and all five allowed operations (`aggregate`, `rank`, race classification, qualifying classification, and event metadata) now have nonzero non-development semantic-threshold denominators; the report regression requires every source and operation denominator to remain nonzero.
- Corrected the reviewed leader semantics from points-descending rank to official `championship_position` ascending while retaining points in the answer. The distinguishing fixture assigns equal points to Oscar Piastri and Lando Norris, official P1 to lexically later Oscar, and P2 to Lando, so the compiler's generic driver-ID tie-break cannot accidentally prove the expected leader.
- Regenerated the committed 13-answer envelope fixture with `npm run golden:snapshot:answer-evaluation`. The real bounded emitter now returns Oscar with `championship_position: 1` for all reviewed leader questions; aggregate tie cases remain independently covered. No emitted row or envelope was written by hand.
- Independent review first identified the accidental lexical tie success and near-duplicate semantic weakness. After the official-position program and distinguishing seed correction, final re-review reported no findings and confirmed the generated fixture is complete.
- Verification passed: `npm run golden:snapshot:answer-evaluation`; `npm run typecheck`; `npm run lint` with 0 errors and 130 warnings; `git diff --check`; and wrapped `npm run test:f1ql` with 349 tests in 47 files. One intermediate wrapped run correctly found stale 25/12 scorer counts; those were updated to the reviewed 26/13 denominators and the complete rerun passed.
- No translated program, live provider, production command, production read/write, migration, grant, deploy, or secret output occurred. Local canonical programs executed only through the approved bounded fixture emitter against its disposable localhost database.
- PARTIAL live/release boundary remains: no independent provider artifact or production least-privilege proof exists. Exact next step: if a provider credential is safely available, collect the non-executing localhost artifact, run `npm run report:answer-evaluation -- <artifact>`, and retain only sanitized aggregate/hash evidence. Separately, production view migration, dedicated role/grants, principal audit, deployment, and any translated execution still require explicit authorization and retained production evidence.

### Phase 7 Live Provider Evaluation: PARTIAL (2026-07-23)
- A first guarded non-executing collection using the retired default Anthropic model completed all 26 observations as `provider_error`; retained artifact SHA-256 `df60dc91a4fb47b3ba3cbb12e30331b50e37c1f4db0962faebef5849d90a5bd0`. A credential-safe account model listing established that supported models were available without printing a key or provider response content.
- A second guarded collection using the account-visible Haiku model completed all 26 cases against the exact disposable localhost database without executing translated programs. Retained artifact SHA-256 is `40dbb684030e6b371585bcfa81c5cfd289dc1b1059af288fa8ec35e41de548fa`; manifest SHA-256 is `34e54121df729cb16ee637fec76bfde65bf6f42a6836a8e5056c8f9734503623`.
- The sanitized report failed release gates: 8/26 actions and 4/26 reasons were correct; 0/13 answer programs were exact; no answers were emitted; there were 9 false abstentions and 4 false clarifications. Candidate recall and complete links were both zero. Every represented holdout source and operation failed its required threshold. Unsafe and forbidden answers remained zero, observations were complete, and all three metamorphic groups were consistently non-answering.
- The low-cardinality outcome aggregate was 9 `program_invalid`, 5 `season_missing` clarifications, 5 `event_ambiguous` clarifications, 2 `entity_ambiguous` clarifications, 2 `capability_unsupported`, and one each of `invalid_response`, `sprint_source_unsupported`, and `constructor_source_unsupported`. The translation prompt documented exact pace shapes but omitted exact canonical shapes for every allowed answer capability; it now includes schema-valid examples for final standings points, official-position leader ranking, named race/qualifying classifications, and race-date metadata, plus deterministic linker/policy ownership. A regression reparses every embedded example through the real candidate schema.
- Verification after the contract correction passed: `npm run typecheck`; `npm run lint` with 0 errors and 130 warnings; and wrapped `npm run test:f1ql` with 350 tests in 47 files. The correction has not been live-evaluated because the provider credit balance was exhausted after collection; a subsequent bounded diagnostic returned only the provider's insufficient-credit status. No secret, raw model output, question, entity, program, SQL, row, or translated execution was retained.
- PARTIAL finishing instructions: replenish the existing provider credit, then from this committed contract run `ANTHROPIC_MODEL=claude-haiku-4-5-20251001 F1QL_ANSWER_EVALUATION_ENABLED=true F1QL_ANSWER_EVALUATION_TARGET=localhost NODE_OPTIONS="-r dotenv/config" npm run collect:answer-evaluation:local`; run `npm run report:answer-evaluation -- <artifact>`; retain only sanitized aggregate/hash evidence. Do not proceed to production or translated execution unless every release gate passes. Production view migration, dedicated role/grants, principal audit, deployment, and any translated execution remain separately blocked on explicit authorization and retained evidence.

### Phase 7 Execution Authorization Boundary: COMPLETE LOCALLY (2026-07-23)
- Added a fail-closed internal principal gate for `POST /program/answer`. The independently rate-limited route requires a separately configured bearer credential of at least 32 characters, compares it with `timingSafeEqual`, and rejects missing configuration or failed authentication before model construction. `.env.example` documents placeholders only; no credential value is logged, returned, or committed.
- Added a versioned request-local execution authorization envelope. It revalidates the program under the active definitions, independently re-authorizes the exact capability, binds the canonical program hash, server-owned request UUID, sole initial `internal` principal class, definitions/compiler/fact-space/work-model versions, and deep-freezes the complete authority object. Inactive definitions, invalid runtime principal classes, mismatched capabilities, invalid request IDs, caller-owned mutation, and post-construction mutation fail closed.
- Request logging now creates a cryptographic UUID and shares it through `res.locals` with the answer authorization boundary. The answer route creates the same server-owned header identity when used without global request logging; caller-supplied request IDs are not authority. The authorization object remains in-memory and is neither returned nor persisted while execution release gates are unsatisfied.
- Focused security review identified inactive-version labeling, disconnected request identities, premature public authorization artifacts, authentication traffic outside the answer limiter, and mutable caller-owned authority data. All findings were fixed; final re-review reported no findings and confirmed the answer route still has no executor import or call.
- Verification passed: `npm run typecheck`; `npm run lint` with 0 errors and 130 warnings; wrapped `npm run test:f1ql` with 357 tests in 48 files; wrapped `npm run test:api:inprocess` with 10 tests; and `git diff --check`. No translated program executed and no production command, database read/write, DDL, grant, migration, deploy, or secret output occurred.
- PARTIAL Phase 7 boundary remains unchanged: a minimal sanitized provider probe still returned HTTP 400, so the corrected translation contract cannot be live-evaluated until provider credit is replenished. Production least-privilege migration/grants/audit and the single reviewed execution remain blocked on explicit authorization. Exact next step: replenish provider credit and rerun the non-executing 26-case collector/report commands above; proceed no further unless all semantic and safety release gates pass.

### Phase 7 Translation Latency Release Gate: COMPLETE LOCALLY / PARTIAL LIVE (2026-07-23)
- The non-executing observation collector now measures only the bounded model-translation call with a monotonic clock and records an integer `translation_latency_ms` on every newly collected observation. Raw negative, non-finite, or over-60-second measurements fail collection; each timed observation is reparsed through the strict artifact schema before insertion.
- The sanitized report now emits only translation-latency count, required count, nearest-rank p95, maximum, fixed budgets, and pass/fail/insufficient status. The conservative release budgets are p95 at most 5000 ms and maximum at most 10000 ms, leaving headroom under the answer route's 12000 ms default request deadline. Complete in-budget evidence passes; missing evidence is insufficient; mathematically conclusive maximum or p95 violations fail even when other timings are absent.
- Backward compatibility is explicit: latency is optional only while parsing historical version-1 artifacts. The real retained 26-case artifact SHA-256 `40dbb684030e6b371585bcfa81c5cfd289dc1b1059af288fa8ec35e41de548fa` was successfully re-reported from nonempty input with 0/26 latency observations and latency status `insufficient`; its existing semantic release status remained `fail`. New collector output always includes timing.
- Focused review found and drove closure of incomplete-evidence masking for known maximum and p95 failures plus sub-millisecond clock regression rounding. Final re-review reported no findings and confirmed nearest-rank p95, sanitization, compatibility, and structural non-execution.
- Verification passed: `npm run typecheck`; `npm run lint` with 0 errors and 130 warnings; wrapped `npm run test:f1ql` with 359 tests in 48 files; `git diff --check`; and the real legacy-artifact report command. No translated program executed and no production command, database read/write, DDL, grant, migration, deploy, or secret output occurred.
- PARTIAL live boundary: no alternate provider credential is configured locally and Anthropic credit remains unavailable, so fresh latency and corrected semantic evidence cannot be collected. Exact next step remains to replenish provider credit and rerun the guarded collector/report; all semantic, safety, linker, and latency gates must pass before any production or execution work.

### Phase 7 Translation Timeout Release Gate: COMPLETE LOCALLY / PARTIAL LIVE (2026-07-23)
- New observation artifacts record `translation_timed_out` for every case. The collector races translation against a fixed 15000 ms deadline, returns a deterministic provider-error abstention even when a provider ignores cancellation, and defers best-effort abort until after the deadline result can return so synchronous abort listeners cannot extend the bound.
- Timeout evidence is causal and strict: a claimed timeout must include at least 15000 ms measured translation latency, must be a `provider_error` abstention, and cannot contain linker candidates or linked entities. Malformed timeout evidence fails artifact parsing.
- The sanitized report adds only observed count, required count, timeout count, the zero-timeout budget, and pass/fail/insufficient status. Any known timeout fails immediately; complete zero-timeout evidence passes; missing historical evidence remains `insufficient`. Timeout status participates independently in the aggregate release status, with failure taking precedence over incomplete evidence.
- The real retained 26-case artifact SHA-256 `40dbb684030e6b371585bcfa81c5cfd289dc1b1059af288fa8ec35e41de548fa` was re-reported from nonempty input with 0/26 timeout observations and timeout status `insufficient`; its existing semantic release status remained `fail`. The report retained no question, case ID, model name, entity, program, SQL, parameter, row, or raw provider output.
- Focused review found and drove closure of non-cooperative provider hangs, timer-versus-provider error misattribution, synchronous abort-listener delay, and contradictory timeout artifact shapes. Final re-review reported no findings and confirmed timer ordering, late-rejection handling, compatibility, privacy, and structural non-execution.
- Verification passed: `npm run typecheck`; `npm run lint` with 0 errors and 131 warnings; wrapped `npm run test:f1ql` with 361 tests in 48 files; `git diff --check`; and the real legacy-artifact report command. No translated program executed and no production command, database read/write, DDL, grant, migration, deploy, or secret output occurred.
- This live-evidence boundary was subsequently resolved with the locally configured Groq provider recorded below; Anthropic credit remains unavailable. All semantic, safety, linker, latency, and zero-timeout gates still must pass before any production or execution work.

### Phase 7 Groq Provider Evaluation: COMPLETE COLLECTION / RELEASE FAILED (2026-07-23)
- A replacement Groq credential configured only in the local environment was mapped process-locally into the existing OpenAI-compatible adapter. A credential-safe model inventory returned HTTP 200, and the guarded localhost collector selected `llama-3.3-70b-versatile` without printing or persisting the credential.
- The collector completed all 26 reviewed cases against the exact disposable localhost database. Retained artifact SHA-256 is `69728b5a94e18d340871622b7b82fb49cd40a5d0b1b73fe08bb73a30feb74f9b`; manifest SHA-256 remains `34e54121df729cb16ee637fec76bfde65bf6f42a6836a8e5056c8f9734503623`. The collector had no executor import and no translated program executed.
- Performance gates passed: all 26 observations carried timing and timeout evidence, p95 translation latency was 827 ms, maximum was 904 ms, and zero translations timed out.
- Semantic and safety release gates failed: 13/26 actions and 5/26 reasons were correct; 4/13 answer programs were exact; 5 answers were emitted, including one unsafe answer in an ambiguity-risk case. Nine required answers were false abstentions, candidate recall was 8/22, complete links were 5/11, and only one of three metamorphic groups was consistent. The forbidden-answer, source-threshold, operation-threshold, linker, and aggregate release gates all failed.
- The sanitized report retained no question, case ID, model credential, entity, program, SQL, parameter, row, or raw provider output. No production command, production read/write, DDL, grant, migration, deploy, or translated execution occurred.
- PARTIAL release boundary: Groq is now available for bounded non-executing evaluation, but translated execution and production work remain blocked. Exact next step is to diagnose the ambiguity false-positive and false abstentions from the retained local artifact without weakening policy, tighten the translation contract, rerun the guarded Groq collector/report, and proceed no further unless every semantic, safety, linker, latency, and zero-timeout gate passes.

### Phase 7 Hardened Answer Release Gate: COMPLETE LOCALLY / RELEASE FAILED (2026-07-24)
- Replaced generic model-authored F1QL authority on `POST /program/answer` with a deterministic question contract, a strict answer-specific intent union, independent read-only identity resolution, reproducible semantic proof, and nine exact server-owned templates. Models can emit only literal question spans and intent labels; they cannot emit canonical IDs or executable F1QL. The answer route still terminates at `gated_non_execution`, and recursive import-graph tests retain the injected throwing-executor invariant.
- The versioned question contract applies NFKC normalization with Unicode code-point spans and deterministically rejects invalid/control/homoglyph, unsupported-source, interim/temporal/team, mixed race/qualifying, negated, quoted, and prompt-injection forms before provider construction. The reviewed corpus now contains 79 cases, 44 required answers, all nine templates, eight metamorphic groups, and adversarial wrong-valid-value/linker cases. Expected output was regenerated only through the real emitter.
- Semantic authority is branded and server-owned: the proof independently binds the normalized question hash, literal mentions, candidate inventories, selected canonical identities, season participation, template variables, exact materialized program, and proof hash. Bounds and execution authorization accept only active verified proofs; authorization is release-bound, expires after five seconds, is single-use, and rechecks the live kill switch both before issuance and at consumption.
- Added Ed25519-signed version-3 evaluation artifacts and release attestations. Signatures use deterministic code-unit canonicalization and canonical padded Base64. Attestations bind release ID, commit, deployment, provider/model, runtime and semantic contract hashes, exact template allowlist, issue/expiry times, and fresh signed provider/principal/production evidence. Stale, replayed, noncanonical, wrong-key, wrong-context, or tampered evidence fails closed.
- Added a production principal audit that runs one `BEGIN READ ONLY` transaction with a 5000 ms local statement timeout, verifies every effective relation privilege, recursive PostgreSQL-14-compatible role membership, exact safe `f1ql_answer` group attributes, and no escalation path. Its signed report exposes only SHA-256 fingerprints of principal/database identity and binds target, deployed commit, deployment/release IDs, and audit time. Production answer DB connections and this audit require a configured trusted CA with hostname verification; URL SSL overrides are stripped.
- Added unapplied `migrations/20260730_f1ql_answer_role_grants.sql`, which defines only an idempotent `NOLOGIN` group role and grants `USAGE` plus `SELECT` on the exact seven answer views. It creates no login/password and does not revoke database-wide `PUBLIC TEMPORARY`; that shared-impact change remains an explicit DBA prerequisite. The release builder hashes this migration and `20260729_f1ql_answer_identity_views.sql` directly from the repository rather than trusting supplied hashes.
- Strict Groq integration is HTTPS-only and permits only `openai/gpt-oss-20b` or `openai/gpt-oss-120b`. Groq's required closed top-level object now wraps the strict `anyOf` intent union; bare/extra/wrong wrappers fail parsing. A credential-safe probe returned HTTP 200 for the exact schema on both models. The evaluation-only collector supports explicit monotonic pre-request pacing outside measured response latency; it never retries or suppresses an actual 429, which remains a signed failing diagnostic.
- The first post-hardening 120B artifact, SHA-256 `83a8f9e273e81c3146cb6af24f459e9d2ead639a142b81dcbd4c76ff82cd43ff`, proved the original root-union schema was rejected: all 58 attempted translations produced `client` diagnostics. After the root-object correction, signed 120B artifact SHA-256 `2b71ea3973cc50ac86d41c930388d9deee63027a3ef2c693d444521bcfea5cab` passed contract/latency/timeout validation but recorded 55 rate limits and three client failures under the unpaced account quota. Neither artifact is release evidence.
- The final quota-paced strict 20B collection completed all 79 cases against only the disposable localhost fixture database. Signed artifact SHA-256 is `46334a3732cbf35dad85e608597861df23a48b99957a87a6c89dbe2b4a2cce7d`; manifest SHA-256 is `6329add9bdf069a0643ce16a46f5fabec7a7ec24257663551d38c6cc11b7817f`. Performance gates passed with 58/58 timings, p95 1430 ms, maximum 1551 ms, and zero timeouts. Unsafe and forbidden answers remained zero.
- The final semantic/provider release gate failed: 32/79 actions and 21/79 reasons were correct; 0/44 required programs were exact; no answers were emitted; there were 43 false abstentions and one false clarification. Candidate recall was 0/53, complete links 0/36, six of eight metamorphic groups were consistent, and every holdout source/operation threshold failed. Diagnostics were 37 `client`, 18 `rate_limit`, and two `schema_invalid`; no proof completed. Release status is `fail`, so no release attestation can be issued.
- Final verification passed `npm run typecheck`; `npm run lint` with 0 errors and 149 warnings; wrapped `npm run test:f1ql` with 552 tests in 56 files; wrapped `npm run test:api:inprocess` with 11 tests; and `git diff --check`.
- PARTIAL production boundary: both migrations remain unapplied; no dedicated login/membership, trusted production CA, production evidence key, principal audit, migration evidence, release key/attestation, deployment, canary, or production round-trip exists. A DBA must separately remove effective `PUBLIC TEMPORARY` and effective `PUBLIC` routine `EXECUTE` from the answer principal without unsafe shared-database impact, or exact safe routines must be explicitly reviewed in a future nonempty allowlist; the current routine allowlist is empty. Provider release also remains blocked until a fresh signed complete artifact has zero diagnostics and passes every semantic, linker, template, proof, metamorphic, latency, and timeout gate. No production command, production database operation, migration, grant, deployment, or translated execution occurred.

### Phase 7 Baseten Provider Gate: COMPLETE / PRODUCTION REMAINS BLOCKED (2026-07-24)
- A locally configured Baseten credential was mapped process-locally into the HTTPS-only OpenAI-compatible adapter without printing or persisting it. The live catalog returned HTTP 200 and exposed all six requested models. A fixed three-case strict-schema probe initially scored every model 0/3, isolating the model-facing contract rather than authentication or endpoint access as the failure.
- Removed model authority over Unicode offsets: strict model output now contains only exact case-sensitive literal text, and the server derives unique Unicode code-point spans before reparsing the complete trusted intent. Repeated array references are assigned only when literal occurrence counts match exactly; missing, duplicated, or ambiguous singular text fails closed. Unique trusted status cues now own status normalization and status-template selection. The intent prompt contains the exact nine-template decision table, literal status mappings, bounded examples, and explicit clarification/unsupported rules. Optional validated `F1QL_ANSWER_REASONING_EFFORT` is explicit provider configuration; Baseten evaluation used `none`.
- Moved cue-less metric ambiguity and narrow adversarial manipulation/order/limit forms into deterministic admission before provider construction. This includes the reviewed `who was last`, `top three`, `also add`, `substitute`, `omit`, `but return`, and `but use` cases. Independent semantic proof remains responsible for the retained non-deterministic wrong-valid-value and repeated-identity attacks. Ambiguous-driver proof failures now retain all bounded canonical candidates, including an already-resolved event, instead of dropping partial independent evidence.
- After correction, GLM 5.2 Fast, Kimi K2.7 Code, GLM 5.2, and Nemotron Ultra scored 3/3 on the same probe. Kimi K2.7 Code then scored 10/10 across all nine supported templates plus clarification with 1134 ms maximum latency and was selected for the full guarded run. Inkling remained malformed and DeepSeek V4 Pro remained incomplete/schema-invalid under the bounded adapter; neither was promoted.
- The final signed non-executing Kimi K2.7 Code collection completed all 79 reviewed cases against only the disposable localhost fixture database. Artifact SHA-256 is `3fd9afabe83ba0b7de162c414cfc1d2ca5b1ea3ebe5139338e0379c3e9a20f99`; manifest SHA-256 is `78ea0852bcf4f1d41c154acb496bd01397873fbfa2a04ad0c1aa36a7bb064087`. Contract bindings are question v12, intent v4, template v2, proof v4, prompt SHA-256 `d99c804fe83a453d59333084a06da0b915b41ab18d7241102b8397589e1c53ea`, and schema SHA-256 `d1db00652f15fb943a207d930fea4ec124bd71d3ca8142bd852db89dfe7e9208`. Signed provider evidence additionally binds the normalized Baseten endpoint SHA-256 `d9b8ebd535d53de06283f6054bc3979a7921b1fdc55edf7d040e02046d35d4b3` and reasoning mode `none`; release verification rejects either binding if active configuration differs.
- Every provider release gate passed: 79/79 actions and reasons, 44/44 exact programs and complete semantic proofs, 53/53 candidate recall, 36/36 canonical links, all nine templates, all eight metamorphic groups, every holdout source/operation threshold, zero unsafe or forbidden answers, zero provider diagnostics, and zero timeouts. Translation latency passed at 1590 ms p95 and 1755 ms maximum across 50 attempted provider calls; 29 cases were deterministic.
- Final verification passed `npm run typecheck`; `npm run lint` with 0 errors and 151 warnings; wrapped `npm run test:f1ql` with 748 tests in 56 files; wrapped `npm run test:api:inprocess` with 11 tests; and `git diff --check`. An earlier wrapped F1QL run hit the known shared-fixture deadlock/missing-relation race; subsequent clean full runs passed without skipped tests.
- Provider evidence is complete, but no release attestation was issued because production evidence remains absent. Both migrations remain unapplied; no dedicated production login/membership, trusted CA, principal audit, signed migration/deployment evidence, release key, deployment, canary, production round-trip, or translated execution occurred. A future production release requires a fresh in-age signed provider artifact plus every existing production evidence and least-privilege gate; this passing local artifact does not authorize execution by itself.

### Phase 7 Production Evidence Workflow: PARTIAL (2026-07-24)
- A sanitized Railway production configuration probe confirmed the Baseten profile and fail-closed runtime flags are set exactly: provider `openai-compatible`, strict schema enabled, reasoning `none`, answer disabled, kill switch enabled, and canary stage zero. The dedicated `F1QL_ANSWER_DATABASE_URL`, trusted database CA, production-evidence keys, and release keys are absent.
- A bounded production catalog probe used the existing application connection only inside `BEGIN READ ONLY` with a 5-second local statement timeout. It confirmed both answer migrations are unapplied, the `f1ql_answer` group role is absent, `PUBLIC` still has database `TEMPORARY`, and 96 non-system routines retain effective `PUBLIC EXECUTE`. The probe emitted only booleans/counts and no database identity, URL, credential, SQL parameter, or row data.
- Added `build:answer-production-evidence`, a dual-flagged offline signer that consumes only a passing, context-matched, signature-verified principal audit. It binds the exact audit bytes, hashed database identities, deployed commit/release context, and repository migration hashes; verifies its own Ed25519 signature; and writes an exclusive mode-0600 artifact through bounded no-follow file operations. Disabled builds, audit tampering/failure, symlink inputs, and existing outputs fail closed.
- Verification passed: `npm run typecheck`; `npm run lint` with 0 errors and 151 existing warnings; wrapped `npm run test:f1ql` with 751 tests in 57 files; wrapped `npm run test:api:inprocess` with 11 tests; and `git diff --check`. No production write, DDL, grant, key generation, deploy, or translated execution occurred.
- PARTIAL finishing instructions: first verify a restorable production backup through the database provider. A DBA must then review the shared impact and remove effective database `TEMPORARY` and non-system routine `EXECUTE` inherited through `PUBLIC`, preserving explicit grants needed by existing callers. Apply `20260729_f1ql_answer_identity_views.sql` and `20260730_f1ql_answer_role_grants.sql` through the approved primary migration channel; create one dedicated `LOGIN` with no dangerous attributes and membership only in `f1ql_answer`; and configure its TLS-verifying URL/CA as `F1QL_ANSWER_DATABASE_URL` and `F1QL_ANSWER_DATABASE_CA_CERT_BASE64`. After those prerequisites, generate separate Ed25519 production-evidence and release keypairs outside the application service, run the signed principal audit, run `build:answer-production-evidence`, build and verify the release attestation, and deploy only with answer disabled, kill switch enabled, and canary stage zero. The existing application `DATABASE_URL` must not be reused as the answer principal, and private signing keys must not remain in the deployed application environment.

### Deterministic CI Legacy ETL Guard Fix (2026-07-23)
- Push workflow run `30050510962` failed in `tests/f1ql/lap-etl-v2-guards.test.ts` because the minimal GitHub runner had no Python `dotenv` package. The disabled legacy FastF1 scripts imported optional ETL dependencies before reaching their fail-closed `main()` guard, so direct execution raised `ModuleNotFoundError` instead of the required safety refusal.
- `src/etl/ingest-laps.py`, `src/etl/ingest-laps-2025.py`, and `src/etl/ingest-laps-2026.py` now refuse direct execution immediately after the standard-library `sys` import, before loading dotenv, FastF1, pandas, or psycopg2. Their existing `main()` refusal remains defense in depth, and module imports used by the v2 idempotency probe are unchanged.
- Verification passed: the exact failed `npm run test:unit:db:docker` gate (820 tests in 67 files), `npm run typecheck`, `npm run test:f1ql` (257 tests in 36 files), and `npm run lint` (0 errors, 123 pre-existing warnings). No production command, write, DDL, deploy, or translated-query execution occurred.

### Phase 5 Expanded Cited Non-Pace Facts (2026-07-23)
- Expanded the bounded production manifest from 23 to 29 authoritative factual checks while retaining its 32-program total cap (three structural plus 29 factual). New final-FIA assertions cover 2025 Australia winner points under the no-fastest-lap-bonus rule, Sainz's zero-lap DNF, Bearman's final-qualifying DNS, Abu Dhabi Piastri P2 and Colapinto P20/zero points, and Russell's recorded P4/319 final standing. Championship totals continue to read only `f1ql.driver_standings`; no total is derived from race points.
- Safe Exa discovery was restricted to FIA documents and stopped when the shared free rate limit was reached. It exposed a source conflict: Australia's provisional race classification labels Hadjar `DNS`, while the final classification labels him `DNF`. The manifest cites only the final document and adds no Hadjar race-DNS assertion. The previously observed Las Vegas DSQ remains outside the manifest because no exact final FIA classification URL plus matching canonical row is retained; no identity/status mapping was invented.
- Added manifest/audit regressions for the 29-check count, 32-program cap, added final-FIA DNF/DNS edges, and the absence of an unverified DSQ assertion. `npm run test:f1ql` passed: 253 tests in 34 files. `npm run lint` completed with 0 errors and 123 pre-existing warnings. No production command, production write, DDL, deploy, or translated-query execution occurred.
- PARTIAL: `npm run typecheck` now fails in the concurrently added, unrelated `src/etl/pace-v2-2025-expansion-manifest.ts` (`version: number` not assignable to literal `1` at line 58). The full suite was non-green at session baseline from transient shared-fixture contention but passed on the serial wrapped retry. Do not commit this expansion until the unrelated typecheck failure is resolved and all required gates are rerun.

### Phase 5 Nightly Production Verification (2026-07-23)
- GitHub Actions run `29978376509` at commit `01b5dab` authenticated with the workspace token after mapping the repository secret to Railway CLI's `RAILWAY_API_TOKEN` and setting the non-secret `RAILWAY_PROJECT_ID`. It executed all guarded production readers and uploaded sanitized artifacts; verification metadata SHA-256 is `1e8428fadbc3b2c1dceab6940f8998cbb76b55d0a05e90048352d9b4b8b0d5bd`.
- Pace preflight was `ready` (9,577 serving rows across 2026 rounds 1-10) and bounded performance evidence was `observed`. The 30-day shadow window contained no retained events.
- The production corpus initially failed three checks. Two manifest expectations were corrected to canonical/source semantics: Carlos Sainz is `carlos-sainz-jr`, and Bearman's FIA DNS qualifying classification retains position 20. Colapinto's final classification/position is retained, but the FIA zero-points assertion is not imposed on F1QL because its canonical race-points field is nullable and currently null; this is a documented field-semantics gap, not a value inferred to zero.
- GitHub Actions rerun `29978676729` at commit `7df1ee4` passed. Its sanitized metadata SHA-256 is `69545784d31b053ef3e21c16c9988621ea23305a49c0a6fc8ecbe2767fdd1c45`: production corpus and authority audit passed; pace preflight was ready; performance evidence was observed. No production write, DDL, deploy, or translated-query execution occurred.

### Phase 5 Production Lifecycle Audit (2026-07-23)
- Guarded production lifecycle audit passed at commit `4e2b80d`. Retained stdout is SHA-256 `1801f7bc3dc4c7c526b558b7e71655f2196db089f69a5cfc37665f074f98b7a7` at `/var/folders/p9/gh5frnt56_l4t03p8dl_fq3m0000gn/T/opencode/database-lifecycle-20260723T041300Z.json`.
- The audit confirmed all expected legacy, v2, correction, audit, and serving relations; `f1ql.lap_pace` served 1,000 `laps_normalized_v2` rows and 8,577 `nat_pit_flags_v1` replacement rows, with no active rebuild rows. It found no recognized public migration-ledger relation, reported that as the documented warning, and did not infer migration history.
- The runner used one connection, `BEGIN READ ONLY`, a 5000 ms transaction-local statement timeout, fixed catalog/read statements, and rollback. No production write, DDL, deploy, or translated-query execution occurred.

### Phase 5 Database Lifecycle Audit (2026-07-22)
- Added `audit:database-lifecycle:production`, a dual-confirmed, loopback-refusing lifecycle observer. It opens one production connection, executes `BEGIN READ ONLY`, applies a 5000 ms transaction-local statement timeout, uses fixed catalog/read queries only, rolls back, and emits exactly one JSON report. It has no DDL, DML, persistent audit sink, translated-query execution, or production repair path.
- The report inventories committed migration SQL filenames and recognizes only explicit public migration ledger relations (`schema_migrations`, `migrations`, or `knex_migrations`), refusing to infer application history from an unknown table shape. It reconciles expected legacy/v2/correction/serving relations and columns, reports relation/table/index sizes, inventories database catalog dependents on legacy `laps_normalized`, observes correction-layer dependencies, retains the deployed `f1ql.lap_pace` definition, and counts active served rows with explicit precedence `fastf1_complete_race_v1` then `nat_pit_flags_v1` then `laps_normalized_v2`.
- Documentation is at `docs/PRODUCTION_DATABASE_LIFECYCLE_AUDIT.md`; regression coverage verifies dual flags, loopback refusal, transaction-local timeout, rollback, fixed non-mutating SQL, unavailable-ledger reporting, and missing serving-view handling. `npx vitest run tests/f1ql/database-lifecycle-audit.test.ts` passed (2 tests).
- PARTIAL: no production command was run and no JSON artifact was retained. The required baseline is non-green before and after this isolated addition: `npm run test:f1ql` fails six suites from concurrent shared fixture setup (`deadlock`, `pg_type_typname_nsp_index`, and missing setup relations), despite `--no-file-parallelism`; `npm run typecheck` fails three TS1355 errors and `npm run lint` fails one `curly` error in `src/etl/pace-v2-2025-expansion-manifest.ts`. Those source errors are outside this audit change and were not modified. Fix those harness/source failures, rerun `npm run typecheck && npm run test:f1ql && npm run lint`, then execute the documented guarded production command, retain the sanitized JSON/hash in `docs/PRODUCTION_EVIDENCE_LEDGER.md`, and commit as `phase-5: audit database lifecycle` only when all gates are green.

### Phase 5 Strict Pace Eligibility-Artifact Attempt (2026-07-23)
- Resolution: FIA's Australian Race History Chart is now acquired by the fixed-URL, evidence-only `fetch:pace-v2:fia-r1-history-artifact` parser. The fresh artifact is 411,114 bytes, SHA-256 `ead4075f84120ec0ce2a5808d1553775a8d970b55398cc895b4870318463b5a1`; it has 794 printed lap-time rows and 30 explicit `PIT` markers (row fingerprint `543f599363229eed3b4b5ee02e91aa4fc893b6e151b55c58216d764a2ec56237`). The parser records only printed lap number, racing number, lap time, leader-relative gap when printed, and `PIT`; it does not calculate a car-ahead gap or derive validity/in/out flags.
- Promotion boundary formalized in `docs/F1QL_SOURCE_CONTRACTS.md` and `docs/F1QL_PRODUCTION_CORPUS.md`: filtered `clean_air_gap_2_0s_v1` pace is coverage-only until one authority carries all mapped per-lap eligibility semantics. F1QL currently has no separately exposed unfiltered raw-timing metric, so no factual pace golden can be added without changing product semantics.
- Selected ready 2026 round 1 (Australian Grand Prix): the retained read-only preflight has 835 serving F1QL-eligible laps. Fresh Formula 1 artifacts were byte-retained only in a mode-0700 OS-temporary directory: `TimingData.jsonStream` (5,607,988 bytes, SHA-256 `a2521be4b468f9ec4c61211558521c993269eb34c24c600e1fa3c90ebb251c8d`) and `DriverList.jsonStream` (16,531 bytes, SHA-256 `ee6c5096ab3c3f477eaf4856ed97cb9457bc0d109e26a90d4dd182df6ab57747`). Fresh FIA PDFs were Race Lap Chart (358,958 bytes, SHA-256 `e09df9ec2dab4ab1c7ed9f8f913826c6bbe7cbd41d132ad472565c2979399270`), Pit Stop Summary (347,810 bytes, SHA-256 `b58492e23eb4f184a9c62444126a806241d81a192acee2081b7703f6a280ceb2`), and On Track Analysis (349,677 bytes, SHA-256 `6ac6c25a4826d7583c70750cfe5311f18928660ebd993f27c452c9098ca76f84`). URLs and r1 DriverList provenance are committed in `data/pace-v2-official-2026-coverage-matrix.json`.
- Strict proof is BLOCKED, not partially inferred. The Lap Chart has per-lap position order only; Pit Stop Summary has driver/lap/time/stop data; On Track Analysis has transponder/pit-exit/laps/pit-entry records. None supplies a per-completed-lap validity decision or numeric gap to the car ahead. `TimingData` has incremental `IntervalToPositionAhead`, `InPit`, and `PitOut` updates, but no reviewed official contract binds an interval snapshot to every completed lap or establishes it as the FastF1 gap used by `clean_air_gap_2_0s_v1`; its pit states likewise do not specify the exact completed lap bearing the F1QL in/out flags. Thus no inclusion or exclusion can be asserted as externally equal for `clean_air_flag`, `is_valid_lap`, `is_pit_lap`, `is_in_lap`, or `is_out_lap` without inventing a mapping.
- No production write, DDL, deploy, shadow-translation execution, or pace golden promotion occurred. Exact completion requirement: retain an FIA/F1 authority artifact (and its field semantics) that keys each completed lap by racing number and lap number with validity, pit/in/out, and numeric car-ahead gap values, then run the guarded read-only per-lap comparison and promote only if every record and identity mapping is equal.

### Phase 5 Nightly Production Verification Workflow (2026-07-22)
- Extended `.github/workflows/f1ql-shadow-review.yml` into a schedule/manual-only production verification workflow. Its protected GitHub `production` environment and concurrency group gate and serialize production observations. It runs the guarded F1QL production corpus, database-authority audit, pace-v2 preflight, and the 30-day shadow report through Railway with only the repository `RAILWAY_TOKEN` secret; PR CI remains production-free.
- Railway CLI distinguishes project tokens (`RAILWAY_TOKEN`) from workspace/account tokens (`RAILWAY_API_TOKEN`). The repository secret is a workspace/account token, so the workflow maps its encrypted `RAILWAY_TOKEN` secret to `RAILWAY_API_TOKEN`; no secret value is stored in the repository or emitted by the workflow.
- Account-token CLI runs also need an explicit project context in fresh GitHub runners. The workflow sets the non-secret `RAILWAY_PROJECT_ID` for `f1muse` while retaining named production-environment and `main` service selection.
- Each run uploads sanitized UTC/commit-tagged artifacts even when a guarded reader fails. Raw Railway logs, database URLs, credentials, and authority URLs are not uploaded. The read-only guards remain in the existing runner scripts; no translator or translated-query execution path was added.
- Verification: `npm run typecheck` passed; `npm run lint` passed with 0 errors and 123 pre-existing warnings; `npx vitest run tests/f1ql/nightly-verification-artifacts.test.ts` passed; and `npm run test:f1ql` passed 241 tests in 30 files. The F1QL wrapper now passes Vitest's explicit `--no-file-parallelism` option because its test files reset one shared fixture database.

### Priority 1 Production Record Reconciliation (2026-07-22)
- Added `docs/PRODUCTION_EVIDENCE_LEDGER.md` as the authoritative index for material production operations, including command, UTC capture point, commit, artifact hash/path, read/write classification, outcome, and bounded evidence scope. Historical gaps are explicitly marked `not retained`; no timestamp, command, or hash is inferred from prose.
- Fresh guarded read-only preflight reported `ready`: `f1ql.lap_pace` serves all ten 2026 rounds with 9,577 v2 source rows; round 1's immutable identity-repair bridge and rounds 2-10's immutable fingerprint-only audit reconciliations pass. The retained stdout report is SHA-256 `62b1423df7a3bc04591d6c64b0e88acd743db3ac464e4e798b304839f6eb1a6e` at the ledger path. Historical ETL `partial_failure` statuses for 2022, 2023, 2025, and 2026 remain warnings, not readiness errors.
- Fresh guarded identity evidence generated the only accepted contract: `version: 2`, `source: "canonical_race_results_fastf1_identity_map"`, season 2026 rounds 2-10, SHA-256 `e69d5784a939da8315a62eea1015cb7c20a275b4edd6e02390d7047a6d63a45e`. `docs/PACE_METHODOLOGY.md` no longer describes the rejected v1 contract.
- Fresh guarded retirement sampling completed with 100 bounded aggregate labels; its retained stdout report is SHA-256 `87b7a5e37f572a62886fcaf63b18757aeb557ef1b049d479dbe82ff89fb51b71`. Known mappings normalize, and unreviewed labels remain `unknown`. This resolves the stale conflict: `WORK_LOG.md` was correct and earlier `PROGRESS.md` statements that sampling was intentionally unrun are superseded.
- No production write, DDL, deploy, or shadow-translation execution occurred during this reconciliation.

### Phase 5 F1QL Source Contracts (2026-07-22)
- Added `docs/F1QL_SOURCE_CONTRACTS.md` version `v1` as the definitive contract for standings, race classification, qualifying classification, event metadata, and lap pace. It specifies relation authority, observed coverage, field/null/status semantics, identity normalization, pace methodology, production factual evidence, unsupported boundaries, and links to the production evidence ledger.
- The contract reconciles to the current ledger: production coverage observations and the bounded 23-check authority audit remain observations rather than whole-database proof; the 2025 absent DNS/DSQ rows remain explicit source gaps; and the round-2 official timing result is raw-lap/identity evidence only, never filtered F1QL pace validation.
- Documentation-only verification: `npm run typecheck` passed and `npm run lint` passed with 0 errors/123 pre-existing warnings. The mandated `npm run test:f1ql` was pre-existingly non-green due to concurrent test-suite setup against one shared database (`pg_extension_name_index` duplicate and missing setup relations in four suites). A retry with `npm run test:f1ql -- --maxWorkers=1` did not forward the worker option through the wrapper and also failed from concurrent teardown/setup (deadlock and missing F1QL views). No source, test, production, or configuration code was changed; fix the wrapped-suite isolation/argument forwarding before committing this documentation milestone.

### Phase 5 Production Database Authority Audit (2026-07-22)
- Added `audit:database-authority:production`: a dual-confirmed, loopback-refusing, single-connection audit that uses `BEGIN READ ONLY`, a 5000 ms transaction-local timeout, fixed read-only statements, and rollback. It inventories calendar/races, classification, standings, qualifying, pace, F1QL-projected identities, and local scoring-rule intervals; it has no mutation, persistent log, or shadow-translation execution path.
- Exa discovery was bounded to FIA and Formula 1 official domains. The retained authority set comprises FIA classification, championship-points, archive, and regulation material plus Formula 1 results/archive material. The report exposes source URLs and a missing/contradiction ledger; `docs/PRODUCTION_DATABASE_AUTHORITY_AUDIT.md` states scope and reproducible command.
- Production round-trip passed: core calendar, race classification, standings, and qualifying have observed season coverage from 1950 through 2026; 2026 calendar has 22 events while currently classified/qualified race coverage has 10 events (220 rows each). Pace has observed coverage only for 2026: 10 events, 22 drivers, 9,577 rows. Race classification contains 860 distinct driver IDs and 186 team IDs. The local scoring registry covers 1950-2026 in 32 intervals.
- All 23 fixed authoritative factual checks passed and the ledger was empty. The sample includes 1950 metadata; 2014 double-points; 2019 fastest-lap; 2021 shortened-race; 2022 scoring; 2024 metadata/classification; and 2025 standings, race, and qualifying. This is quantified sample evidence, not verification of every row or document. Pace remains coverage-only except for the separately retained narrow official raw-lap artifacts; no clean-air, pit, in-lap, or out-lap authority is claimed.
- Verification passed: `npm run typecheck`; `npm run test:f1ql` (240 tests in 29 files); `npm run lint` (0 errors, 123 pre-existing warnings); and the guarded production run. No production write or deploy occurred.

### Phase 5 Local Compiler Benchmark (2026-07-22)
- Added `benchmark:f1ql:compiler`, a bounded local-only benchmark over the committed 100-case corpus. It reports p50/p95 for parse, surface validation, lowering, core validation, and compilation together, plus independent reference-interpreter execution against non-empty in-memory fixture rows for every supported source. It has no Pool, database URL, network, or production path, makes no latency assertions, and is excluded from tests/CI.
- Local result: 25 warmup and 200 measured corpus passes accepted 89 programs and emitted 90 fixture rows per pass. Compile/lower/validate p50/p95 was 4.18/13.62 ms per corpus pass; fixture execution p50/p95 was 0.10/0.46 ms per corpus pass. These are local observational measurements, not release thresholds.

### Phase 5 Official Pace Validation Layers 1-3 (2026-07-22)
- Extended the guarded validator with the official round-2 `DriverList.jsonStream` artifact. Its retained provenance is `https://livetiming.formula1.com/static/2026/2026-03-15_Chinese_Grand_Prix/2026-03-15_Race/DriverList.jsonStream`, 18,351 bytes, SHA-256 `7849c324df1cfddf363dc65c0f90e06c0ae57c4df68430034fb814b1919f41e5`. A mapping is accepted only when its official racing number exactly matches canonical `race_data.driver_number` and its official TLA exactly matches canonical `driver.abbreviation`; ambiguous, missing, or mismatched values refuse before comparison.
- Production round-2 read-only result: both official artifacts passed their committed hashes. All 22 official DriverList racing numbers mapped exactly to 22 canonical driver IDs, including the 18 v2 drivers. Of 816 raw v2 driver/laps, 815 are exactly numerically equal to their official completed timing record and none differ. One comparison is blocked rather than inferred: `carlos-sainz-jr` / racing number `55` / lap `49` has v2 `98.022` seconds but no official completed raw-lap record. The official stream has 900 completed raw laps, leaving 85 official records without a v2 raw-lap counterpart; this is coverage, not a mismatch. The command used one `BEGIN READ ONLY` transaction, a 5000 ms local timeout, and rollback; no production write occurred.
- Clean-air, pit, in-lap, and out-lap metadata remain unavailable from the official artifacts. The validator emits `unavailable_not_inferred` for every record and makes no eligibility, filtered-pace, or causal claim from raw timing equality.
- Added `validate:pace-v2:official-layers:production`, a dual-flagged loopback-refusing read-only validator. It accepts a retained official timing file only when its SHA-256 matches the committed 2026 coverage matrix, then uses one `BEGIN READ ONLY` transaction with a five-second local statement timeout to observe the selected v2 event. It has no writer, migration, or production mutation path.
- Layer 1 reports official observed racing-number and v2 driver counts. Layer 2 parses completed raw official timing laps and reports their count with v2 raw-lap coverage. Layer 3 emits per-v2-lap eligibility evidence and precise v2 exclusion reasons for missing time, invalid, pit, in-lap, and out-lap flags.
- The per-lap observation reads `laps_normalized_v2` directly, not `f1ql.lap_pace`, because the deployed serving view does not expose `lap_number`; it remains a raw-v2 validation and does not alter either relation.
- Validated case: provenance hash and raw timing parser are enforced. Unverified cases: driver coverage, raw per-driver/lap times, and official eligibility comparisons remain unverified because retained TimingData identifies drivers only by racing number and supplies none of the reviewed clean-air/pit/in-lap/out-lap fields; the validator emits `unavailable_not_inferred` rather than deriving them. A hashed reviewed racing-number-to-v2-driver mapping plus shared eligibility authority is required to promote those layers.
- Production read-only result: the retained round-2 artifact (`380259ce59c9e7b5b81aa4872106e2a3ba9e476fd7129d31f27fcc45aa2b747d`, 5,248,701 bytes) passed provenance validation. It observed 22 official racing numbers (18 with completed timing) against 18 v2 drivers, 900 official completed raw laps against 816 v2 laps, and emitted 816 v2 per-lap evidence records. The transaction used `BEGIN READ ONLY`, a 5000 ms local timeout, and rollback; no production write occurred. These are coverage observations only, not matched driver or pace facts.

### Phase 5 Bounded Pace Artifact Inventory, Rounds 1-10 (2026-07-22)
- Added `fetch:pace-v2:official-inventory-2026`, an evidence-only, fixed-allowlist Formula 1 collector. It tries only the ten reviewed 2026 `TimingData.jsonStream` URLs and, where timing is retained, the matching `WeatherData`, `RaceControlMessages`, and `TimingAppData` streams. Successful streams are byte-preserved in fresh mode-0600 OS-temporary files and hashed; unavailable timing candidates are reported with their URL and no invented content or context classification. It has no database, fact-generation, production, or writer path.
- The emitted matrix is committed at `data/pace-v2-official-2026-coverage-matrix.json`. Timing is retained for r1 `20/22`, r2 `18/22`, r3 `22/22`, r6 `22/22`, r7 `21/22`, r8 `21/22`, and r10 `22/22` timed/observed drivers; reviewed r4, r5, and r9 URLs returned HTTP 403 and are explicitly unavailable. New timing SHA-256 values are r8 `12fd07cfc682961bc905c4a648fa25a6041a5fdf8da388282ee9a9a25ff5c0fc` and r10 `c4f5963de9f5779bd736beabf51fc56a7a72cbdb3409b3083501827daf4fc175`; all timing/context URLs and hashes are in the committed matrix.
- Literal metadata classification finds no rainfall observation for r1/r2/r3/r7/r8/r10, rainfall observed for r6, safety-car deployment observed for r2/r3/r6/r8, and no literal `RETIRED` race-control message in retained streams. Pit-heavy remains unestablished because incremental stint updates are not a reviewed pit-stop interpretation. V2 eligible-driver coverage is deliberately `not_assessed`: a factual overlap requires a separate read-only v2 observation plus a reviewed racing-number mapping, and these artifacts lack the shared clean-air/pit/in/out eligibility fields. No median or F1QL pace claim is made.
- Verification passed: `npm run typecheck`; `npm run lint` (0 errors, 123 pre-existing warnings); and `npm run test:f1ql` (236 tests in 27 files). No production database was read or written.

### Phase 5 Multi-Round Official Raw Timing Validation (2026-07-22)
- Added `fetch:pace-v2:official-timing-2026`, a fixed-allowlist Formula 1 collector for 2026 rounds 1 (Australian), 2 (Chinese), 3 (Japanese), 6 (Miami), and 7 (Canadian). It allows only the five exact HTTPS `TimingData.jsonStream` URLs, validates the timing-stream signature before retention, writes each byte-preserved stream to a fresh mode-0600 OS-temporary path, and emits source URL, retrieval UTC, SHA-256, bytes, per-driver timed-lap fingerprints, and raw median summaries. It has no database, production, fact generation, or writer path.
- The 2026-07-22 collection retained r1 at `2026-07-22T22:16:53.083Z`, 5,607,988 bytes, SHA-256 `a2521be4b468f9ec4c61211558521c993269eb34c24c600e1fa3c90ebb251c8d`, 20 timed drivers; r2 at `2026-07-22T22:16:55.644Z`, 5,248,701 bytes, SHA-256 `380259ce59c9e7b5b81aa4872106e2a3ba9e476fd7129d31f27fcc45aa2b747d`, 18; r3 at `2026-07-22T22:16:59.734Z`, 7,024,902 bytes, SHA-256 `90581624eb501ce980bcfcaf6b0d9666dda7be82a5cf4a39f1bc75c90b536bab`, 22; r6 at `2026-07-22T22:17:03.488Z`, 6,329,475 bytes, SHA-256 `8f095d21d41706a1c6ea7e69dd8f3dfd51eb7883e990579d974f6a93b0f536ee`, 22; and r7 at `2026-07-22T22:17:05.555Z`, 5,787,515 bytes, SHA-256 `8b177bb8d267945985b40bfe14b7bcff0a68941ee2e5633a3c050e182825eb9b`, 21. The mode-0600 raw paths and full reports are in the command output from that run.
- Coverage targets are r1 normal/dry, r2 retirement-limited, r3 wet/disrupted, r6 pit-heavy, and r7 current-season. These are intentionally not asserted factual condition classifications: the reviewed `TimingData` fields cannot establish weather, disruption cause, retirement status, pit eligibility, clean-air gaps, in laps, or out laps. The only validated facts are completed `NumberOfLaps`/`LastLapTime.Value` and `Stints.LapNumber`/`Stints.LapTime` timing pairs, deduplicated by lap number and summarized as raw per-driver medians.
- F1QL clean-air/pit-filtered pace comparison remains unsupported because no externally retained authority artifact supplies a reviewed driver mapping plus the same clean-air/pit/in/out fields. No pace production golden was added; `docs/F1QL_PRODUCTION_CORPUS.md` records why. Existing `validate:pace-v2:event:production` remains the dual-flagged, loopback-refusing, five-second-timeout, `BEGIN READ ONLY` observational runner and must not be treated as external validation.
- Verification: `npm run typecheck`; `npm run lint` (0 errors, 123 pre-existing warnings); `npm run test:f1ql` (231 tests in 27 files); and the five-source collector passed. No production database was accessed or modified.
- Next step: obtain and retain an official per-lap artifact for one selected round that includes reviewed driver identity, clean-air-gap, pit, in-lap, and out-lap fields. Hash it, extend the fixed allowlist/parser only for those documented fields, then run the existing guarded read-only event observation for that exact round and add a pace production golden only if every compared field and identity mapping is externally supported.

### Phase 5 Multi-Event Official Context Artifact Validation (2026-07-22)
- Added `fetch:pace-v2:official-context-2026`, which uses the existing exact timing-archive allowlist to fetch only the matching Formula 1 `WeatherData`, `RaceControlMessages`, and `TimingAppData` streams. It validates framing before writing each stream to a fresh mode-0600 OS-temporary file and emits the URL, UTC retrieval, byte count, and SHA-256. It has no database or fact-writing path and refuses an unallowlisted event before issuing a request.
- The 2026-07-22 context collection established literal official fields: r1 has 148 rainfall samples all not `"1"` and no `SAFETY CAR DEPLOYED`; r2 has 152 rainfall samples all not `"1"` and one deployment; r3 has 156 rainfall samples all not `"1"` and one deployment; r6 has 168 samples including `"1"` and one deployment; r7 has 160 rainfall samples all not `"1"` and no deployment. The raw `TimingAppData` streams emit repeated incremental stint-record updates only; they are retained and fingerprinted but are not treated as a pit-stop or pit-eligibility fact.
- Provenance SHA-256 triples in `WeatherData` / `RaceControlMessages` / `TimingAppData` order are r1 `e729c63508be2a47610bd39d74f1290b474e2ed2677407165ac087296f126ccb` / `0847b77ce9eadfb1285cca6605798f2374f698bffd2014f56279b3fb527c03b1` / `07815b4cafb6683e49ab9c4e672ba7247355eb1870fefec3c67fbfb1b64f7631`; r2 `67e2f3187fd84e5246d391d13fc81f758b0fd3cfa3d9261c04684cdd0351d272` / `4d1180c0f4c71a7cf7584013b32eb20583c1f98699f0d565f4928f4d973af717` / `8fc490723feb8fcd9cef67fb099e3c9c5ff3c8ea63c1330046fa8a7826c0ac25`; r3 `ebdf0d3199681b2e3573b2c52f3ff22a7d18154b5d0a521ce42e7af53d16c916` / `bcb01ad42716e656544b717befa170a6a384d0e8de07a2576b4b67a4a2035854` / `8572e157adea911a5554c62b1ed97d421c4e68d4b31244f8ffc9a033554bd325`; r6 `018aa17ec024d1ee9ce23725976761463b7299bd4d669a340ba759b6a84dc9aa` / `c04c3e259fea7131b1ccb53d58ab6dd11b44db8da7a82eba7bf5704543572d58` / `151bcf45198cd16b2b11094efcb41078ad700645851b07f9bda07090c66f1515`; and r7 `17e12b153b0f84d9b0e0e65f0f80d1d07a52afb475e1fe6f2df581651015504d` / `19b1cd3003ebbd0dbfb93af1f28d8cecba71bc95cff61e276065fac260d63274` / `01db9349efbfb3e39384978daf5d2d2882f6198691311c29b978848700a36b23`.
- Retirements, pit stops, clean-air gaps, in/out laps, and a cross-source driver identity mapping remain unsupported. No F1QL pace comparison or production golden is permitted from these context artifacts; the existing production runner remains observation-only, read-only, timeout-bound, and dual-flagged.

### Phase 5 Round-2 Official Lap Timing Artifact Protocol (2026-07-22)
- Located the official Formula 1 historical `TimingData.jsonStream` archive for the 2026 Chinese Grand Prix race: `https://livetiming.formula1.com/static/2026/2026-03-15_Chinese_Grand_Prix/2026-03-15_Race/TimingData.jsonStream`. Mediated retrieval demonstrated that it contains individual `LapTime` and `LapNumber` records, unlike the FIA final-classification PDF. The Formula 1 results page independently identifies the event as the 13-15 March 2026 Chinese Grand Prix and publishes only fastest laps, not the individual-lap series.
- Added `fetch:pace-v2:round2-lap-timing-artifact`, an evidence-only fixed-URL collector. It validates HTTPS/host/path and the timing-stream signature, writes a new mode-0600 temporary raw artifact, hashes it, and emits individual-lap fingerprints plus deterministic raw timed-lap medians for Norris (`1`), Bortoleto (`5`), Albon (`23`), and Piastri (`81`). It has no database, fact-generation, writer, or production path. The median protocol deduplicates final `(lap_number, seconds)` values, sorts seconds, and takes the usual odd/even median.
- Provenance artifact retained: direct system-TLS acquisition at `2026-07-22T17:24:42.772Z` wrote `/var/folders/p9/gh5frnt56_l4t03p8dl_fq3m0000gn/T/pace-v2-round2-f1-timing-yxtDKI/TimingData.jsonStream` mode `0600`, 5,248,701 bytes, SHA-256 `380259ce59c9e7b5b81aa4872106e2a3ba9e476fd7129d31f27fcc45aa2b747d`. The official endpoint's response metadata was HTTP 200, ETag `"c1a273bebbbca3f16d4cd582b1e65343"`, and Last-Modified `2026-03-15T08:59:59Z`. Its UTF-8 BOM and CRLF framing are retained byte-for-byte; the parser normalizes only those framing markers in memory.
- Limitation: the artifact's `TimingData` stream contains sector updates but no completed `NumberOfLaps`/`LastLapTime.Value` pair for any of the four required racing numbers. It therefore reports `required_driver_timing_coverage: "incomplete"` and zero timed-lap entries/`null` raw medians, rather than asserting a timing value. This is a source-coverage limitation, not evidence a driver did not participate. It also lacks a reviewed durable mapping for the application's complete clean-air/pit/in/out eligibility inputs, so no `f1ql.lap_pace` validation, individual-lap comparison, raw-median, or factual pace assertion is made.
- The direct Node client was unable to consume the same source because of the BOM signature mismatch; the evidence-only collector now uses the available system TLS client with fixed official-site headers, exact URL validation, and the stream signature check. It has no database, fact-generation, writer, or production path.
- Verification: `npm run typecheck`; `npm run test:f1ql` (230 tests in 27 files); `npm run lint` (0 errors, 123 pre-existing warnings). No production access, query, deployment, or write occurred.

### Phase 4 Pace Event Artifact Diagnosis (2026-07-22)
- Guarded production round-2 execution initially refused generically. A read-only reproduction isolated PostgreSQL SQLSTATE `42703`: the `eligible_lap_driver_median` query attempted to cast its `median_lap_time_seconds` SELECT alias in the same query's `ORDER BY`, where PostgreSQL cannot resolve that alias.
- The artifact now computes medians in a CTE before ordering, reads the serving `f1ql.lap_pace` relation rather than bypassing approved replacement selection, and emits typed non-sensitive refusals. Median query failures identify `reason: "event_median_query_failed"`, `predicate: "eligible_lap_driver_median"`, and a validated SQLSTATE; configuration, argument, and residual runtime failures have separate reasons. Regression coverage injects the exact `42703` failure.
- Guarded production rerun: `railway run --no-local --service main --environment production env PACE_V2_EVENT_ARTIFACT_ENABLED=true PACE_V2_EVENT_ARTIFACT_TARGET=production npm run --silent validate:pace-v2:event:production -- 2026 2`. It used the existing read-only transaction, five-second local timeout, and rollback; no production write occurred. Result: `observed`, 18 Shanghai driver medians, observation fingerprint `c2851439a9eeb29159eefbd3426f59c570b08af86396d6b0779b73419dbdc869`.
- Verification passed: `npm run typecheck`; `npm run test:f1ql` (227 tests in 26 files); `npm run lint` (0 errors, 123 pre-existing warnings).

### Phase 4 Approved NaT Pace Preflight Diagnosis (2026-07-22)
- Guarded production reads used a single `BEGIN READ ONLY` transaction, a five-second local statement timeout, and rollback; no production write occurred. They proved `f1ql.lap_pace` selects the approved `nat_pit_flags_v1` facts for every 2026 round 2-10: selected/original/replacement eligible-lap counts are r2 `816/0/816`, r3 `923/0/923`, r4 `858/0/858`, r5 `1045/0/1045`, r6 `1177/0/1177`, r7 `1035/0/1035`, r8 `1164/0/1164`, r9 `856/0/856`, and r10 `703/0/703`. Each source has the same per-round row count.
- The production preflight was nevertheless reporting `round_without_eligible_laps` for all nine rounds because its coverage query read `laps_normalized_v2` directly. Its immutable-audit checks correctly remain original-fact checks: each round has only `manifest_fact_fingerprint` false, but all are accepted by exact immutable `manifest_fact_fingerprint_only_v1` reconciliation. The only current production conditions are the nine false original-eligibility warnings and historical ETL `partial_failure` status warnings for 2022, 2023, 2025, and 2026.
- Preflight coverage now reads the serving selection `f1ql.lap_pace`, reports `pace_selection_relation: "f1ql.lap_pace"`, and fails explicitly if that relation is absent. It retains direct original-v2 reads only for immutable manifest/reconciliation verification, so approved replacement facts cannot silently alter the original evidence contract. No deployment or production write was performed.
- A follow-up guarded production read confirmed every serving eligibility predicate passes: r1 has `1000/835` total/eligible laps, and r2-r10 have `816/816`, `923/923`, `858/858`, `1045/1045`, `1177/1177`, `1035/1035`, `1164/1164`, `856/856`, and `703/703`. Round 1's identity-repair bridge and rounds 2-10's immutable `manifest_fact_fingerprint_only_v1` reconciliations pass every predicate. The persistent non-ready report was a status-reducer bug: it promoted historical non-blocking ETL `partial_failure` warnings (2022, 2023, 2025, 2026) to `attention`. It now returns `ready` unless an error-severity condition exists; warnings remain in the JSON report. No production write occurred.
- The preflight CLI now emits exactly one structured JSON record. A post-readiness pool-close failure no longer appends the generic refusal after a valid `ready` report; actual wrapper failures retain non-sensitive typed reasons for configuration and runtime categories. Regression coverage injects the close failure and verifies the ready wrapper output. No production command or write was run.

### Phase 4 Corrected FastF1 DNS Filtering (2026-07-21)
- The canonical v2 FastF1 identity-map evidence now carries a sorted `official_non_starter_codes` list per round. The corrected-facts generator removes only those explicit canonical DNS/W codes from the FastF1 payload before driver-code resolution; every other unresolved FastF1 code, including an actual starter, continues to refuse closed.
- The Round-2 regression models canonical DNS codes `NOR`, `ALB`, `BOR`, and `PIA`, proves they are excluded before normalization without changing the 90 expected facts, and proves `NOR` refuses when it is not explicitly canonical-excluded. Identity-map regressions verify official exclusions are preserved separately from starter mappings. No production query or write was run.
- Verification passed: `npm run typecheck`; `npm run test:f1ql` (223 tests in 26 files); `npm run lint` (0 errors, 123 pre-existing warnings). Read-only evidence retry: `PACE_V2_NAT_IDENTITY_MAP_ENABLED=true PACE_V2_NAT_IDENTITY_MAP_TARGET=production npm run --silent generate:pace-v2:nat-identity-map:production`; retain the reported `output`, then set `OUTPUT_DIR="$(mktemp -d /tmp/pace-v2-nat-corrected-facts.XXXXXX)"` and run `npm run --silent generate:pace-v2:nat-corrected-facts -- --manifest /approved/evidence/pace-v2-nat-replacement-manifest.json --identity-map <reported-identity-map-output> --output "$OUTPUT_DIR/corrected-facts.json"`. This evidence generation performs no production write.

### Phase 4 NaT Identity-Map Canonical Starter Alignment (2026-07-21)
- The read-only NaT FastF1 identity-map generator now reads all canonical `race`/`race_data` result identities and derives its emitted expected code set exclusively from canonical starters. Explicit trimmed, case-insensitive `DNS`, `DID NOT START`, `W`, `WD`, and `WITHDRAWN` values in either authoritative status field are retained only as official FastF1-code exclusions; persisted v2 facts are not queried.
- Round 2's 18 canonical starters now reconcile safely with FastF1's 22 codes when its four additional codes are official non-starters. The generated identity map contains only the 18 starter identities. Any missing starter or FastF1 code not backed by a canonical starter or explicit official exclusion remains a typed fail-closed refusal.
- Regressions cover the 18-versus-22 DNS/W case, unrecognized FastF1-code refusal, canonical-only SQL, and existing ambiguity checks. Verification passed: `npm run typecheck`; `npm run lint` (0 errors, 123 pre-existing warnings); `npm run test:f1ql` (223 tests in 26 files). No production query or write was run.

### Phase 4 NaT Pace Artifact Contract Alignment (2026-07-21)
- Diagnosed the unsupported-shape refusal: `generate:pace-v2:nat-identity-map:production` emits `{ version: 2, source: "canonical_race_results_fastf1_identity_map", season: 2026, rounds }`, but `generate:pace-v2:nat-corrected-facts` was still restricted to the superseded `{ version: 1, source: "approved_fastf1_identity_map", ... }` contract. The identical round/track/code-to-driver mapping payload was rejected before FastF1 fact generation.
- The corrected-facts parser now imports and accepts only the canonical v2 identity-map type and discriminator. It continues to fail closed for a legacy v1 artifact, wrong source, season, round scope/order, invalid track, code, or driver ID. The corrected-facts regression supplies the current producer shape through full normalized fact generation and asserts that v1 is rejected. No production query or write was run.
- Production read-only retry after independent manifest review: run `PACE_V2_NAT_IDENTITY_MAP_ENABLED=true PACE_V2_NAT_IDENTITY_MAP_TARGET=production npm run --silent generate:pace-v2:nat-identity-map:production`, retain its reported `output` path, then set `OUTPUT_DIR="$(mktemp -d /tmp/pace-v2-nat-corrected-facts.XXXXXX)"` before running `npm run --silent generate:pace-v2:nat-corrected-facts -- --manifest /approved/evidence/pace-v2-nat-replacement-manifest.json --identity-map <reported-identity-map-output> --output "$OUTPUT_DIR/corrected-facts.json"`. This generates evidence only; it does not perform a production write.

### Phase 4 Explicit Non-Starter Pace Coverage (2026-07-21)
- The canonical pace-rebuild starter predicate now excludes an explicit trimmed, case-insensitive `position_text` or `race_reason_retired` token of `DNS`, `DID NOT START`, `W`, `WD`, or `WITHDRAWN`, irrespective of `race_laps`, including `NULL`. This applies identically to the read-only incomplete manifest, read-only FastF1 identity map, and serializable rebuild provenance check.
- The regenerated Round-2 fixture models both status fields for every token with `race_laps IS NULL`, retains a null-lap formation-lap DNF, and confirms exactly 18 canonical actual starters. No production query or write was run.

### Phase 4 Superseded Zero-Lap Non-Starter Contract (2026-07-21)
- The prior zero-race-lap condition was superseded by the explicit-status contract above: authoritative `position_text` or retirement-reason non-starter tokens exclude a driver regardless of `race_laps`. Actual starters, including formation-lap DNFs, remain canonical.
- The earlier local zero-lap fixtures remain historical coverage; the current Round-2 fixture covers every explicit status token in both fields with null lap counts.
- Added forward-only `20260727_normalize_f1ql_nonstarter_statuses.sql`: explicit `W`/`WD`/`WITHDRAWN` position or reason tokens normalize to `withdrawn`; explicit `DNS`/`DID NOT START` normalize to `dns`. No production migration, query, deployment, or write was run.
- Verification passed: `npm run typecheck`; `npm run lint` (0 errors, 123 pre-existing warnings); `npm run test:f1ql` (221 tests in 26 files).

### Phase 4 Corrected Round-2 Rebuild Manifest Diagnosis (2026-07-21)
- The generic Round-2 manifest refusal after `c5c871b` was caused by the non-starter predicate's SQL three-valued-logic bug: `NOT (rd.race_laps = 0 AND status)` evaluates to `NULL` for unknown `race_laps`, which a `WHERE` clause rejects. It therefore removed the four `race_laps IS NULL` status rows as if they were confirmed zero-lap non-starters, reducing canonical coverage from 22 to the 18 persisted drivers; the required `canonical_coverage_incomplete` predicate then failed but the CLI collapsed it into `pace_v2_incomplete_rebuild_manifest_failed`.
- Replaced the unsafe predicate in the manifest generator, identity-map generator, and serializable writer with `rd.race_laps IS NOT DISTINCT FROM 0`: only a confirmed numeric zero can exclude an explicit withdrawal/DNS. Unknown lap counts stay canonical. The manifest CLI now emits the typed `incomplete_rebuild_candidate_invalid` refusal with all five named candidate predicates. Regressions cover both an unknown-lap withdrawal remaining canonical and the exact candidate predicate refusal.
- Guarded production execution used the existing one-connection `BEGIN READ ONLY` transaction, five-second local statement timeout, and rollback. It now generated the exact approved Round-2 manifest: 816 original facts, 22 canonical drivers, original fingerprint `f2cc348a49dfcbd2c71c30dcc15af2fd362879068661ab7ce077b4e794cf2280`, canonical fingerprint `9b1ff55709c1194af4ac6f14d90854114a69f9512489f57590bf7de5d3aa8858`, manifest fingerprint `e9585e9ad82d97b8fb5e85e3eaed567972a73738db09ac1d202c0308bf6c720d`. No production write occurred.

### Phase 4 Incomplete Rebuild Facts Round-2 Diagnosis (2026-07-21)
- The approved Round-2 manifest (`e9585e9ad82d97b8fb5e85e3eaed567972a73738db09ac1d202c0308bf6c720d`) and canonical identity map parse successfully. Reproducing the generator locally against its FastF1 Race payload performed no database access or production write and refused because canonical coverage is incomplete: 22 drivers were approved, but normalization emitted 816 facts for 18 drivers. Missing canonical IDs are `alexander-albon`, `gabriel-bortoleto`, `lando-norris`, and `oscar-piastri`.
- Each missing driver has one FastF1 source row with a positive lap number but no valid timing, so the existing normalizer correctly excludes it. The pit-flag poison safeguard passes; this is not an identity-map mismatch or a `pd.isna` regression. No partial facts artifact was emitted.
- Replaced the generic generator refusal with the safe typed output `incomplete_canonical_driver_coverage`, including round, expected/observed driver counts, and sorted missing canonical IDs. A mocked nonempty Race payload regression proves the exact refusal without emitting partial facts. Verification passed: `npm run typecheck`; `npm run lint` (0 errors, 123 pre-existing warnings); `npm run test:f1ql` (218 tests in 25 files).
- Next step: obtain independently reviewed authoritative timing for the four Round-2 drivers, or amend the approved canonical coverage contract with documented authority. Do not bypass the coverage check, generate a partial artifact, or perform a primary rebuild until one of those evidence paths is complete.

### Phase 4 Round-2 Timing Artifact Investigation (2026-07-21)
- Fresh local FastF1 Race inspection for 2026 round 2 (`Chinese Grand Prix`, 924 source rows) confirms the prior diagnosis: `ALB`, `BOR`, `NOR`, and `PIA` each have exactly one positive lap-number row (lap 1) and zero finite lap-time rows. The extractor is not dropping a valid FastF1 time; its normalizer correctly excludes each null-time row.
- A bounded Exa lookup located FIA Document 76, `2026 Chinese Grand Prix - Final Race Classification`, at `https://www.fia.com/system/files/decision-document/2026_chinese_grand_prix_-_final_race_classification.pdf`. The FIA index describes it as the final timing-sheet classification. It confirms race participation, but it is not lap-by-lap timing and cannot supply or justify an invented individual lap time. The missing records are therefore a FastF1 source-artifact gap, not evidence the drivers had no valid timed race lap.
- Added `fetch:pace-v2:round2-timing-artifact`: fixed FIA URL only, HTTPS/FIA-host/PDF magic validation, new mode-0600 OS-temporary artifact, SHA-256/byte/retrieval report, and non-numeric inspection of the four required driver names. It performs no database operation and has no facts/writer integration. Tests cover URL/PDF refusal, hash, permissions, and inspection. Exact next step: acquire a separately retained FIA/F1 official lap-analysis or timing-feed artifact that demonstrates a timed lap for each driver; record its URL/retrieval UTC/SHA-256 and review mappings before regenerating an identity map or facts. If unavailable, retain the incomplete-round refusal.
- The bounded fetch completed at `2026-07-22T05:59:09.181Z`: 695,756-byte PDF, SHA-256 `c576a8ebc3d6523a8cb83fdf4888a5e28a3b3369bc4d8c4e26447baefa05e730`. The retained mode-0600 temp artifact inspected positive for all four names. No production access, database query, fact generation, or write was performed.

### Phase 4 Incomplete Pace-Round Rebuild (2026-07-21)
- The incomplete-rebuild manifest now requires an explicit non-empty, unique approved subset of 2026 rounds 2-10 (`--rounds 2` for the pilot); it cannot default to all rounds. Generated facts are bound to that exact manifest fingerprint and can contain facts for exactly its selected rounds. The serializable writer reparses both contracts, rejects unbound/mismatched scope before opening its transaction, and preserves the existing original-fact, canonical-driver identity/fingerprint, FastF1 identity-map fingerprint, immutable replacement, and immutable audit contracts.
- Added a round-2-only regression proving a one-round manifest produces five replacement facts/audit inputs and no query is issued for round 3. Local verification passed: `npm run typecheck`; `npm run lint` (0 errors, 123 pre-existing warnings); `npm run test:f1ql` (217 tests in 25 files). No production command, query, migration, deployment, or write was run.
- PARTIAL authorized round-2 evidence/pilot commands: `PACE_V2_INCOMPLETE_REBUILD_MANIFEST_ENABLED=true PACE_V2_INCOMPLETE_REBUILD_MANIFEST_TARGET=production npm run --silent generate:pace-v2:incomplete-rebuild-manifest:production -- --rounds 2 > /approved/evidence/pace-v2-round2-incomplete-rebuild-manifest.json`; generate/review the canonical identity map using `PACE_V2_NAT_IDENTITY_MAP_ENABLED=true PACE_V2_NAT_IDENTITY_MAP_TARGET=production npm run --silent generate:pace-v2:nat-identity-map:production`; then `npm run --silent generate:pace-v2:incomplete-rebuild-facts -- --manifest /approved/evidence/pace-v2-round2-incomplete-rebuild-manifest.json --identity-map <identity-map.json> > /approved/evidence/pace-v2-round2-incomplete-rebuild-facts.json`. After independent review and separate write authorization only: `PACE_V2_INCOMPLETE_REBUILD_ENABLED=true PACE_V2_INCOMPLETE_REBUILD_TARGET=primary npm run --silent rebuild:pace-v2:incomplete-rounds -- --manifest /approved/evidence/pace-v2-round2-incomplete-rebuild-manifest.json --facts /approved/evidence/pace-v2-round2-incomplete-rebuild-facts.json`.
- Added the separately versioned `fastf1_complete_race_v1` rebuild path for only approved 2026 race rounds 2-10. It does not weaken the original NaT replacement contract: original `laps_normalized_v2` facts and every prior audit remain untouched. The new immutable fact/audit relations retain original fact count/fingerprint, canonical race-result driver count/fingerprint, full replacement fingerprint/count, manifest fingerprint, and FastF1 identity-map fingerprint.
- The identity map now derives its exact code-to-driver mapping only from canonical `race`/`race_data` race results plus FastF1 Race payloads, not persisted v2 rows. It fails closed on ambiguous/missing canonical identities, tracks, codes, or FastF1 mismatch. Regression coverage models the four canonical-only round-2 drivers: `ALB -> alexander-albon`, `BOR -> gabriel-bortoleto`, `NOR -> lando-norris`, and `PIA -> oscar-piastri`.
- `f1ql.lap_pace` selects a rebuild only after its immutable audit exists; otherwise it preserves existing replacement/original selection. The serializable primary writer uses a five-second local timeout, validates current partial original and canonical contracts, rejects duplicate approval or incomplete FastF1 coverage, and has no update/delete statement. Tests assert no mutation of production v2 facts. Local verification passed: `npm run typecheck`; `npm run lint` (0 errors, 123 pre-existing warnings); `npm run test:f1ql` (216 tests in 25 files). No production command, query, migration, or write was run.
- PARTIAL production steps: apply `20260726_pace_v2_incomplete_rebuild.sql` through the approved primary migration channel. In an authorized read-only production environment, generate and independently retain/review the canonical map with `PACE_V2_NAT_IDENTITY_MAP_ENABLED=true PACE_V2_NAT_IDENTITY_MAP_TARGET=production npm run --silent generate:pace-v2:nat-identity-map:production`, and the incomplete manifest with `PACE_V2_INCOMPLETE_REBUILD_MANIFEST_ENABLED=true PACE_V2_INCOMPLETE_REBUILD_MANIFEST_TARGET=production npm run --silent generate:pace-v2:incomplete-rebuild-manifest:production > /approved/evidence/pace-v2-incomplete-rebuild-manifest.json`. Generate/review the complete FastF1 facts using `npm run --silent generate:pace-v2:incomplete-rebuild-facts -- --manifest /approved/evidence/pace-v2-incomplete-rebuild-manifest.json --identity-map <identity-map.json> > /approved/evidence/pace-v2-incomplete-rebuild-facts.json`; retain SHA-256 evidence for all three. Only after separate authorization, a primary operator may run `PACE_V2_INCOMPLETE_REBUILD_ENABLED=true PACE_V2_INCOMPLETE_REBUILD_TARGET=primary npm run --silent rebuild:pace-v2:incomplete-rounds -- --manifest /approved/evidence/pace-v2-incomplete-rebuild-manifest.json --facts /approved/evidence/pace-v2-incomplete-rebuild-facts.json`. Then run a guarded read-only preflight and retain its output; investigate any refusal without retrying or mutating originals.

### Phase 4 NaT Identity-Map Round-2 Diagnosis (2026-07-21)
- Guarded production identity-map generation used the existing loopback-refusing, one-transaction `BEGIN READ ONLY` path with a five-second local statement timeout and rollback. It refused at 2026 round 2 before generating an artifact because the FastF1 driver-code set and the canonical v2/race driver-code set differ; no production data was modified.
- Exact round-2 evidence: FastF1 has 22 codes and the v2 identity query has 18. FastF1-only codes are `ALB`, `BOR`, `NOR`, and `PIA`, whose canonical race-result IDs are respectively `alexander-albon`, `gabriel-bortoleto`, `lando-norris`, and `oscar-piastri`; there are no database-only code/ID pairs. All four canonical IDs are present in round-2 `race_data` results but absent from both `laps_normalized_v2` and legacy `laps_normalized`. The root cause is therefore incomplete persisted lap-fact coverage, not an ambiguous identity or abbreviation mapping. The retained data cannot establish the historical ingestion condition that omitted them; do not invent one or bypass the coverage failure.
- The original refusal omitted the required set members. It now reports sorted `missing_database_codes` (FastF1-only) and `extra_database_identities` (database-only exact code/driver ID pairs), alongside both counts, while retaining the fail-closed behavior. Regression coverage verifies both directions.

### Phase 4 NaT Pit-Flag Replacement Facts (2026-07-21)
- Added the reviewed forward-only `20260725_pace_v2_nat_replacement.sql` path for only 2026 race rounds 2-10. It preserves `laps_normalized_v2` and all previous audit relations unchanged, creates separately versioned `nat_pit_flags_v1` facts, and records immutable manifest/original/replacement-fingerprint approval evidence. Replacement facts are immutable after insertion; their audit is immutable; no fact can be appended once a round is approved.
- `f1ql.lap_pace` now selects an approved replacement only for an audited affected round and otherwise selects the original v2 facts. The replacement writer is serializable, five-second timeout scoped, exact-manifest scoped, primary-only, validates the original all-three-NaT poison class/fingerprint/count and exact lap identities, rejects an unchanged poison artifact, and inserts facts plus audit atomically. The read-only manifest generator is dual-flagged, loopback-refusing, bounded to the exact nine reviewed rounds, and rolls back.
- Added local tests for healthy-original fallback, approved replacement selection, manifest/artifact fail-closed validation, and no-original-fact mutation. Production was intentionally not run. Before any authorized production action: apply `20260725_pace_v2_nat_replacement.sql` through the approved primary migration channel; generate/review and retain the read-only manifest; generate/review a fixed-`pd.isna` corrected-facts artifact with source evidence; run the separately authorized primary command; retain both SHA-256 artifacts and stdout evidence.

### Phase 4 Controlled NaT Corrected-Facts Generation (2026-07-21)
- Diagnosed the local artifact gap: neither an approved nine-round replacement manifest nor an approved FastF1 identity-map artifact is present in the workspace, and the previous implementation had no corrected-facts generator. Therefore no genuine artifact can be generated or validated locally without inventing source evidence; no production access or write was attempted.
- Added `generate:pace-v2:nat-corrected-facts`. It reuses the fixed `pd.isna` FastF1 payload extractor and canonical pace normalization, accepts an immutable approved manifest as its only round selector, and requires an independently approved exact rounds-2-10 track/driver identity map. It writes only a newly-created, mode-0600 JSON artifact under the OS temporary directory, validates it with the writer's parser, rejects count/identity/scope drift and unchanged all-three-flag poison classes, and emits per-round corrected row counts/fingerprints plus artifact/source SHA-256 evidence on stdout.
- The generator was proven against nonempty mocked FastF1 race payloads for all nine rounds: 90 corrected facts retained zero all-three-NaT flags. Local verification passed: `npm run typecheck`, `npm run test:f1ql` (211 tests in 23 files), and `npm run lint` (0 errors, 122 pre-existing warnings). PARTIAL: from an authorized evidence environment, create/review the manifest and identity-map artifacts, run the documented generator to a fresh temp path, review its stdout and hashes, then only a separately authorized primary operator may execute the documented `replace:pace-v2:nat-pit-flags` command with that exact temp artifact. Do not perform a production write automatically.

### Phase 4 NaT FastF1 Identity Map (2026-07-21)
- Added `generate:pace-v2:nat-identity-map:production`, a dual-flagged, loopback-refusing, read-only evidence generator for exactly 2026 race rounds 2-10. It uses one five-second-timeout `BEGIN READ ONLY` transaction over v2 facts joined to canonical `race`, `race_data`, and `driver` identities, always rolls back, and performs no database write.
- It fetches only the corresponding FastF1 Race metadata/lap payloads and emits an identity map only when each exact FastF1 code has one canonical DB driver, all v2 driver identities are race-result identities, each round has one matching canonical/v2 track, and the FastF1 and DB driver-code counts agree. Ambiguous, missing, invalid, count, and track mismatches refuse.
- The only generated artifact is a newly-created mode-0600 `identity-map.json` below the OS temporary directory; stdout is a one-object report with its absolute path and SHA-256. It is accepted directly by `generate:pace-v2:nat-corrected-facts`. Production was intentionally not run. From an authorized read-only production environment run `PACE_V2_NAT_IDENTITY_MAP_ENABLED=true PACE_V2_NAT_IDENTITY_MAP_TARGET=production npm run --silent generate:pace-v2:nat-identity-map:production`; retain the temp artifact and report before using its output path for corrected-facts evidence.

### Phase 4 NaT Replacement Refusal Diagnosis (2026-07-21)
- Production read-only diagnosis confirmed the reviewed migration is present with all three enabled immutability triggers and zero replacement facts/audits. The current complete poisoned originals remain exactly the generated manifest: rounds 2-10 have counts `816, 923, 858, 1045, 1177, 1035, 1164, 856, 703`; its immutable contract fingerprint is `379a20815cae6c3d36d6aaa2f8e2bb6a650d4ed9bfc699f376f9c701e4f756dd`.
- The writer refusal occurred before transaction validation: the documented `npm run ... > artifact.json` form emitted npm lifecycle banners before the JSON, so the approved manifest artifact was invalid JSON. The writer previously collapsed that parse error into the generic `pace_v2_nat_replacement_failed` output. No database write committed.
- Fixed the evidence commands to use `npm run --silent`, added parse-safe typed refusal reasons (`manifest_json_invalid`, `replacement_facts_json_invalid`, and contract/preflight categories), and added a regression for npm-polluted evidence. A fresh manifest must be generated with the corrected command and independently approved before any separately authorized replacement attempt; do not reuse the contaminated artifact.
- Follow-up read-only production diagnosis after the formatting fix: the regenerated manifest exactly matches the retained poisoned originals, both replacement relations exist, all three required triggers are enabled (`O`), replacement fact/audit rows are both `0`, the database is primary (`pg_is_in_recovery = false`), and the production role has all required source/replacement `SELECT` and replacement/audit `INSERT` privileges. Thus no persisted writer predicate fails. The prior generic `replacement_preflight_refused` carried no inspectable failing predicate; it is now `replacement_runtime_failure`, while explicit configuration and PostgreSQL permission/serialization/timeout/duplicate-key cases have distinct non-sensitive codes. The corrected-facts artifact was not available in this workspace, so its identity and corrected-flag predicates cannot be re-evaluated without that independently retained input; no production write was attempted.

### Phase 4 Pace V2 Production Preflight (2026-07-21)
- Added `preflight:pace-v2:production`, a translator-free, dual-flag production preflight. It refuses localhost/loopback targets, uses one `BEGIN READ ONLY` transaction with a five-second transaction-local timeout, performs only bounded reads, always rolls back, and emits one JSON stdout object.
- The report covers v2 row count, rows by session type/methodology version, season/round coverage, active-methodology eligible laps, explicit missing/partial conditions, and `etl_runs_laps_normalized` freshness/status when that optional audit relation exists. It has no ingestion or write path.
- Production execution remains intentionally unrun. From an authorized production environment, run `PACE_V2_PREFLIGHT_ENABLED=true PACE_V2_PREFLIGHT_TARGET=production npm run preflight:pace-v2:production > /approved/evidence/pace-v2-preflight-$(date -u +%Y%m%dT%H%M%SZ).json` and retain the stdout artifact outside the database.

### Phase 4 Pace V2 Ingestion Guard Repair (2026-07-21)
- Audited every updated lap ETL. The TypeScript 2026 season ingester already hashes and checks only `laps_normalized_v2`; the three FastF1 Python ingesters (`ingest-laps.py`, `ingest-laps-2025.py`, and `ingest-laps-2026.py`) had stale legacy schema/loaded-round checks despite writing v2 facts.
- Repaired all Python checks to require `laps_normalized_v2` including `session_type` and `methodology_version`, and to determine an already-loaded round only from v2 facts scoped to `(season, round, session_type)`. Legacy `laps_normalized` rows cannot skip v2 ingestion.
- Added a mocked regression that invokes each Python guard: a legacy-populated/v2-empty round returns not loaded, while a v2-populated round returns loaded. It also asserts session scoping and no legacy query. Local-only verification passed: `npm run typecheck`; `npm run lint` (0 errors, 117 pre-existing warnings); and `npm run test:f1ql` (178 tests in 15 files). No production access, ingestion, or production write was performed.

### Phase 4 Pace V2 Manifest Safeguards (2026-07-21)
- Added a read-only, five-second-timeout manifest generator that selects only calendar rounds older than 24 hours with at least ten `race_data` race-result rows. Its fingerprint covers the approved round list; the generated file is the explicit input required by the 2026 pace writer.
- Automatic FastF1 execution was removed from `auto-sync` and legacy lap package scripts were removed. The writer checks persisted session state before contacting FastF1, refuses calendar drift and mismatched full-round fingerprints, uses insert-only facts, and records manifest/source/fact fingerprints and row count in the same transaction.
- Added reviewed forward-only migration `20260722_pace_v2_manifest_audit.sql` for immutable `pace_v2_round_audit`; audit failure is transaction-fatal. Existing pilot v2 facts remain untouched and can only receive a matching immutable audit record on an approved rerun.
- Verification: `npm run typecheck`, `npm run lint` (0 errors, 118 pre-existing warnings), `npm run test:f1ql` (182 tests in 16 files), and `npm run test:api:inprocess` (7 tests) passed. PARTIAL production: no migration, production write, or production query was run. Apply `20260721_pace_correctness_v2.sql` then `20260722_pace_v2_manifest_audit.sql` through an approved primary migration channel; generate/review the manifest with a read-only role; then run the manifest writer with an approved write role and retain manifest/output evidence outside the database.

### Phase 4 Manifest Safety Fixes (2026-07-21)
- Added the reviewed, exact-only pace-v2 source identity reconciliation `australian_grand_prix -> melbourne`. It is applied at manifest/calendar comparison before any FastF1 request; all unlisted identities remain unchanged, with no heuristic or alias fallback.
- The writer now halts on the first failed outcome or thrown round error and reports the remaining approved rounds as unprocessed. Regression tests prove no later round is attempted.
- Safe remediation decision for the existing round-1 pilot mismatch: do not mutate or delete production facts automatically. The persisted `australian_grand_prix` identity fails closed against canonical `melbourne`; require a reviewed reconciliation plan with an explicit, separately authorized repair path, rollback procedure, and retained evidence before any production action. Verification passed: `npm run typecheck`; `npm run lint` (0 errors, 118 pre-existing warnings); and `npm run test:f1ql` (186 tests in 16 files). No production query or write was run for this change.

### Phase 4 Repair And Pace Artifact Protocol (2026-07-21)
- Added `repair:pace-v2:identity`, a completely separate primary-only repair command for the one reviewed v2 round-1 alias `australian_grand_prix -> melbourne`. It requires explicit enable/target flags and a fingerprinted manifest that is restricted to 2026 round 1, `R`, the active methodology, exact alias, positive fact count, and both source/target complete-row fingerprints. It rejects every other mismatch before mutation.
- The repair takes one serializable transaction with a five-second transaction-local timeout, locks the complete affected fact set, validates the source row/fingerprint contract, makes its sole track-ID update, validates the target fingerprint, then inserts immutable evidence in the same transaction. Added forward-only `20260723_pace_v2_identity_repair_audit.sql`; no existing audit or fact is mutated automatically.
- Added `validate:pace-v2:event:production`, a dual-flagged, loopback-refusing read-only artifact command for one selected event. It reports at most 30 active-methodology eligible-lap driver medians with an observation fingerprint, and explicitly labels output `database_observation_only` and external truth as unverified without a separate retained authority artifact.
- PARTIAL production: no repair manifest has been approved, no migration or repair has run, and no production query/deploy was performed. To complete the repair, apply the audit migration through an approved primary channel; produce and independently review the exact one-round source/target fingerprint manifest from a read-only evidence extract; run the explicit command with a primary operator; retain manifest/stdout SHA-256 evidence. To validate pace facts, run the guarded event artifact command and retain a separate authoritative source artifact before making any factual claim.

### Phase 4 Pace V2 Identity Repair Manifest Generator (2026-07-21)
- Added `generate:pace-v2:identity-repair:production`, a separate dual-flagged production evidence command. It rejects localhost/loopback targets, uses one `BEGIN READ ONLY` transaction with a five-second transaction-local timeout, reads only the complete fixed 2026 round-1 race fact set, and always rolls back.
- It fails closed unless every row is the active-methodology exact source alias `australian_grand_prix`; then it derives source and prospective `melbourne` target fingerprints through `fingerprintPaceV2FactRows` and the complete manifest fingerprint through `createPaceV2IdentityRepairManifest`/its canonical parser. It emits one manifest JSON object and has no mutation path.
- Local verification passed: `npm run typecheck`; `npm run lint` (0 errors, 119 pre-existing warnings); and `npm run test:f1ql` (195 tests in 19 files). Production was intentionally not run. From an authorized read-only production environment, run `PACE_V2_IDENTITY_REPAIR_MANIFEST_ENABLED=true PACE_V2_IDENTITY_REPAIR_MANIFEST_TARGET=production npm run generate:pace-v2:identity-repair:production > /approved/evidence/pace-v2-round-1-identity-repair.json`, independently review the result, and retain it before any separately authorized primary repair.

### Phase 4 Pace V2 Audited Repair Readiness (2026-07-21)
- Fixed preflight readiness after the ten-round v2 coverage audit identified round 1 as lacking a normal manifest-audit row despite a completed identity repair. The guarded read-only preflight now checks every complete race fact set against its current fact fingerprint, row count, and active methodology.
- A matching immutable `pace_v2_round_audit` remains the normal acceptance path. The only bridge is the fixed 2026 round-1 repair with an enabled identity-repair immutability trigger and an exact reconstructed `track_identity_exact_alias_v1` repair-manifest fingerprint whose target fingerprint, count, and methodology match current complete facts. A present but mismatched manifest audit never falls through to the bridge, and all missing/invalid contracts remain errors.
- Added preflight regression coverage for accepted bridges and rejected target-fingerprint, manifest-audit, and immutability failures; documented the evidence rule in `docs/PACE_METHODOLOGY.md`. Production was not run.

### Phase 4 Pace V2 Preflight Diagnosis (2026-07-21)
- Ran the guarded preflight through `railway run --no-local --service main --environment production` with both explicit flags. It used the existing one `BEGIN READ ONLY` transaction, five-second local timeout, and rollback; no production write or deployment occurred.
- Production has 9,577 active-methodology race facts across 2026 rounds 1-10. Round 1 is accepted by the immutable identity-repair bridge: 1,000 facts and target fingerprint `977dde08e4d245cf971622c6b51d6742bc8ccb2fd5c3b96adef3145a6bc223ed` match the audit.
- The exact blocking predicate for every round 2-10 is only `manifest_fact_fingerprint`; session type `R`, fact row count, audit methodology, and current fact methodology all match. Current/persisted fingerprint pairs are: r2 `f2cc348a49dfcbd2c71c30dcc15af2fd362879068661ab7ce077b4e794cf2280`/`edc4545127902a060a3d6fdc7cd6deef2e0a7313f37d14447710780f7b6bef0f`; r3 `2d33f06e82e0490222615ee8fb14921cbaec7706fbcc9a1af469e4600b714840`/`ad0c195cbc24e59a9a69fbd84e9f6beab51b6b61dcb4f7d39932625e8809e1d5`; r4 `776c648f47ac9fe5295b07b8641391c3daba7598a61aac61b5073525aeb77bcc`/`c41c4250e22aa8b976d88dca67a47222bc21b00b0f1ad939376fcdb6d062c1c3`; r5 `0165392194da5afa74825493e4f2bc7c888ec27070eea95dec631ff2f09c419e`/`6e508d99c4d3e4111d78295cbb6119f7fa27c2a953dfac851f6c776b57af2f00`; r6 `30eec5da72eb53f3e7169be7f9dc3a84b898995faac938d4ac45f9b4e6508b4e`/`54d71d7a554aaca923d0bb001adde899ae0bb8a2c79bff5a24ee72acb6753ffd`; r7 `099cb72cfcf24018ee39c25cf809ebc982cb64a0cb6c28de29a0a78752cf3cec`/`a44a00c668bba4e21d8cc8636a168d0742bb7be22a87632557495e93c7ce3086`; r8 `e6a5cf77aeb77dc8a1e8057fca5bf5c5961ff32f128877f6f68aec545a270469`/`b452d5becee314fa78df8b5543173688d9f1c59c9264066754bc5de31ff8b910`; r9 `72e8076be9333ab6de423567055fd10f7947f3d890720806b5f3bf3c04d97cb8`/`e5d7d75db3d109ece6f46ae700a2009ad35955e60e22b9f8c6e5fc2bad448aa8`; r10 `80a65afdd327f8efcfc6661d4e1a4c25682499873aecbcab908d9d310d1101ed`/`40b6f37836aac90d13644eda3c8f7fb2f0217a7ed015ab92abee04e647e7462e`.
- Rounds 2-10 additionally have zero eligible laps (row counts: 816, 923, 858, 1,045, 1,177, 1,035, 1,164, 856, 703). This is a warning, not the refusing error. No data or audit mutation is safe without separately reviewed evidence reconciling the immutable manifest fingerprints.
- Preflight JSON now emits every acceptance predicate with expected value, observed value, and pass/fail state for normal manifest audits and the repair bridge; warnings also report their observed counts/statuses. Added regression coverage. Verification passed: `npm run typecheck`; `npm run lint` (0 errors, 119 pre-existing warnings); and `npm run test:f1ql` (197 tests in 19 files).

### Phase 4 Pace V2 Eligibility Diagnosis (2026-07-21)
- Guarded production reads confirmed that rounds 2-10 contain 9,577 active-methodology race facts with non-null times and `is_valid_lap = true`, but every fact has `is_pit_lap = true`, `is_in_lap = true`, and `is_out_lap = true`; therefore the unchanged pace eligibility predicate correctly yields zero laps. Round 1 is healthy (835 standard-eligible laps).
- The current 2026 FastF1 extractor caused this: it interpreted pandas `NaT` absent pit timestamps as present through `value is not None`. It now uses `pd.isna` to treat `NaT` as absent; a regression test locks that contract. No pace predicate, audit acceptance rule, or production data was relaxed or mutated.
- Production preflight now accepts the immutable fingerprint reconciliations for rounds 2-10. The prior manifest-fingerprint issue is no longer refusing readiness; the remaining condition is only the zero-eligible-lap warning caused by corrupted persisted flags.
- Safe remediation requires an approved, separately authorized fact correction/backfill for rounds 2-10 using the fixed writer, with fresh immutable fact fingerprints and audited reconciliation evidence. Do not alter existing facts, audit rows, or acceptance predicates in place.

### CI Test Database URLs (2026-07-21)
- Fixed the `deterministic-tests` SASL-password regression without introducing secrets: the workflow, test wrappers, and `validate-all.sh --with-db` now explicitly override `DATABASE_URL`, `DATABASE_URL_TEST`, and `TEST_DATABASE_URL` with the disposable Docker PostgreSQL URL (`postgresql://postgres:postgres@127.0.0.1:5433/f1muse_test`). This matches `docker-compose.test.yml`'s public fixture credentials and isolates the test database from local port-5432 listeners, preventing database-backed imports that read `DATABASE_URL` from receiving an absent production URL. No production database was accessed or modified.
- Verification: an `env -i` run with no inherited database URL passed `npm run typecheck`, `npm run lint`, `test:unit:db:docker` (737), `test:golden:db` (26), `test:integration:db` (86), `test:api:inprocess` (7), and `test:f1ql` (174). `test:schema:db` retains its unrelated pre-existing failure: the committed production snapshot does not include the locally introduced `laps_normalized_v2` columns. No production access occurred.

### CI Production Schema Snapshot Contract (2026-07-21)
- Refreshed `tests/schema/snapshots/production-schema.json` only through `railway run --no-local --service main --environment production npm run schema:snapshot:production`. The guarded generator used `BEGIN READ ONLY`, a transaction-local 10-second timeout, `information_schema` reads, and rollback; no production schema or data was altered.
- The approved `laps_normalized_v2` migration is now represented by its table entry, 18 captured columns, and session-inclusive six-column primary key. The snapshot also records the database's current ordering of otherwise equivalent unique constraints.
- Documentation now states the guarded snapshot boundary in `docs/PACE_METHODOLOGY.md`. CI-equivalent verification passed: `npm run typecheck`, `npm run lint` (0 errors, 117 pre-existing warnings), `test:unit:db:docker` (737), `test:golden:db` (26), `test:integration:db` (86), `test:schema:db` (28), `test:api:inprocess` (7), and `test:f1ql` (174).

### Pace And Classification Factual Correctness (2026-07-21)
- Added forward-only migration `migrations/20260721_pace_correctness_v2.sql`. It leaves `laps_normalized` untouched, creates `laps_normalized_v2` keyed by `(season, round, track_id, driver_id, session_type, lap_number)`, and switches `f1ql.lap_pace` to v2 only. No production DDL or writes were run.
- Canonical event classification now gives trimmed explicit `position_text` `DSQ`/`DISQUALIFIED` and `DNS`/`DID NOT START` tokens precedence over generic/stale reasons. It deliberately does not infer a DNS from grid/lap data, preserving formation-lap DNFs unless an authoritative correction is ingested.
- F1QL pace accepts only `session_type = 'R'`, active `clean_air_gap_2_0s_v1` rows, and at least two eligible laps before a per-event median contributes. Pace summary and delta rows expose `methodology_version`; unversioned or incompatible rows fail closed by yielding no pace sample. Definitions version is `v2`.
- Updated all four supported lap ETLs to write v2 keys and the active methodology version; the 2025/2026 Python paths now use the matching 2.0-second threshold. `docs/PACE_METHODOLOGY.md` records the rules, explicit historical backfill boundary, and a guarded read-only production coverage/freshness evidence protocol.
- Exhaustive local regressions cover DSQ/DNS precedence, formation-lap DNF, session-key coexistence, race-only filtering, methodology exclusion, sample threshold, compiler/interpreter parity, generated corpus fixtures, and HTTP output. Verification: `npm run typecheck`; `npm run lint` (0 errors, 117 pre-existing warnings); `npm run test:f1ql` (174 tests in 13 files); and `npm run test:api:inprocess` (7 tests) passed. Production verification remains PARTIAL pending authorized migration application, v2 ingestion/backfill, and the documented read-only freshness artifact.

### Phase 5 Property Backbone (2026-07-21)
- Added `fast-check` and `tests/f1ql/core-properties.test.ts`. A deterministic 100-run bounded generator emits only signature-approved generic core IR shapes for standings, event and qualifying classification, event metadata, lap-pace summaries, and lap-pace deltas. Generated limits are 1-20, requested round lists are capped at six (under the Phase 2 24-round budget), and the largest generated pipeline has seven nodes (under the Phase 2 12-node budget).
- Each generated program passes `validateCoreProgram`, compiles without execution, and asserts read-only `SELECT`/`WITH` SQL with no write/DDL keywords, no statement delimiter, and a parameter count matching generated placeholders. No production access or execution was performed; the existing Docker-backed F1QL fixture wrapper remains the only execution path.
- Verification: `npm run typecheck` passed; `npm run lint` passed with 0 errors and 117 pre-existing warnings; `npm run test:f1ql` passed 68 tests in 10 files; `npm run test:api:inprocess` passed 7 tests. No deployment was performed.
- PARTIAL production factual proof: `golden:f1ql:production` was authorized but safely refused in this shell because `DATABASE_URL` is absent; no database query ran. Run `F1QL_PRODUCTION_GOLDEN_ENABLED=true F1QL_PRODUCTION_GOLDEN_TARGET=production npm run golden:f1ql:production` from the authorized Railway service shell or CI environment where `DATABASE_URL` is injected, save stdout outside the database, and record each checked fact/mismatch here.
- Production golden execution attempted through Railway on 2026-07-21 after the database URL redeploy. It connected but failed before fact comparison with `relation "f1ql.event_metadata" does not exist`. The Phase 4 forward migrations are not applied to the shared production database. PARTIAL: do not apply DDL without explicit instruction. After an authorized migration deployment, rerun the identical Railway command and record the JSON facts.
- Authorized migration review and application completed: production base columns matched both reviewed views; `f1ql.event_metadata` and `f1ql.qualifying_classification` were applied transactionally and verified with `to_regclass`. The first production golden run caught an incorrect expected fact: 2024 Bahrain winner Max Verstappen earned 26 points (including fastest-lap bonus), not 25. The manifest is corrected to 26; rerun pending.
- Production golden rerun passed: FIA-cited 2024 Bahrain winner matched Max Verstappen, P1, 26 points, classified; Bahrain event metadata matched name/date/session scope. PostgreSQL numeric `26.00` is normalized against the authoritative numeric fact by a covered comparer; the initial mismatch therefore improved the protocol rather than masking a data discrepancy.

### Phase 5 Versioned Scoring Rules (2026-07-21)
- Added `src/scoring/rules.ts`, a cited, immutable FIA rules registry for 2021-2026 only. It models the standard P1-P10 race schedule, 2021's P1-P3 sprint trial, the 2022-2026 P1-P8 sprint schedule, the 2021-2024 top-10 fastest-lap point, and its 2025 removal. Seasons before 2021 and after 2026 resolve explicitly as unsupported; special-race reductions and older eras are intentionally not inferred.
- `CHAMPIONSHIP_TOTALS_AUTHORITY` names `season_driver_standing`; the registry exposes individual session schedule lookups only and has no total-derivation API. The production Bahrain 2024 golden now references the matching cited 2022-2024 rule version, and manifest initialization rejects a mismatched rule version.
- `docs/SCORING_RULES.md` records the FIA Issue 13 (2021), Issue 7 (2024), and Issue 5 (2025) sources and the boundary policy. `tests/fixtures/scoring-rules-golden.json` is emitted from the real registry by `golden:snapshot:scoring-rules`; exhaustive transition, coverage, authority, eligibility, removal, and fixture tests are in `tests/scoring-rules.test.ts`.
- Verification: `npm run golden:snapshot:scoring-rules` passed; `npm run typecheck` passed; `npm run lint` passed with 0 errors and 117 existing warnings; `npm run test:f1ql` passed 168 tests in 12 files; `npm run test:api:inprocess` passed 7 tests. No production query or deployment was performed.

### Phase 5 Historical Scoring Registry (2026-07-21)
- Expanded the versioned Drivers' Championship race-result registry from 2021-2026 to every inclusive season from 1950 through 2026. Each interval has a source: the FIA sporting regulations for 2021-2026 and the cited StatsF1 historical scoring archive for the digitisation gap before 2021.
- The registry models standard schedules, fastest-lap eligibility and tied-fastest-lap sharing, 1950-1957 shared-drive splitting, every annual dropped-score interval, shortened-race policy boundaries, 2014's final-race double-points exception, and both sprint eras. It deliberately contains no season-total calculation; `season_driver_standing` remains the sole standings authority.
- Explicit limits: event-specific reduced-distance classifications (including 2021 Belgium), penalties/classification amendments, entrant eligibility, and constructor scoring are not inferred. Entries without a cited general shortened-race policy are represented as `unknown`; actual events require an official FIA final classification.
- Regenerated `tests/fixtures/scoring-rules-golden.json` exclusively through the real registry emitter. New tests cover all 77 seasons and both unsupported boundaries, historical schedule and sharing transitions, dropped-score/shortened/double-points boundaries, citations, sprints, and the no-derived-standings invariant.
- Verification: `npm run golden:snapshot:scoring-rules`, `npm run typecheck`, and `npx vitest run tests/scoring-rules.test.ts` passed; `npm run lint` passed with 0 errors and 117 pre-existing warnings; `npm run test:f1ql` passed 168 tests in 12 files. No production data was accessed or changed.

### Phase 5 Production Corpus Audit And Runner (2026-07-21)
- Audited all 100 local golden cases in committed code: 41 are explicitly fixture-only (15 intended rejections and 26 source-dependent lap-pace cases), and 59 are production-runnable structural cases. The synthetic fixture contains no authoritative factual case; the fixed five-case production projection adds two FIA-backed 2024 Bahrain factual checks plus three structural canonical-view checks.
- Reworked `golden:f1ql:production` into the guarded production corpus runner. It requires both explicit production flags, rejects localhost/IPv4/IPv6 loopback URLs, uses one connection and one `BEGIN READ ONLY` transaction with a 5-second transaction-local timeout, permits only bounded compiled read-only statements, preflights each required `f1ql` relation with `to_regclass`, always rolls back, and emits exactly one stdout JSON object. Missing production views are explicit `missing_production_view` skips; fixture-only corpus cases are explicit audit skips. It imports no translator and never executes translated queries.
- `docs/F1QL_PRODUCTION_CORPUS.md` records execution requirements and remaining factual gaps: historical/shared/dropped scoring eras, 2014 double points, 2019-24 fastest-lap versus 2025 removal, sprint transitions, null/status handling, identity aliases, and historical qualifying/lap coverage. Production execution remains intentionally unrun.

### Phase 5 Production Factual-Correctness Protocol (2026-07-21)
- Added `golden:f1ql:production`, a production-only, dual-flag-gated golden runner over the committed two-case `scripts/f1ql-production-golden-manifest.ts`. Its facts are restricted to the completed 2024 Bahrain Grand Prix race result and metadata and cite the FIA 2024 championship document index. Expected facts are compared as explicit authoritative field subsets; mutable internal IDs and free-text status fields are not asserted.
- The runner rejects missing or incorrect `F1QL_PRODUCTION_GOLDEN_ENABLED=true` and `F1QL_PRODUCTION_GOLDEN_TARGET=production` flags and localhost/loopback database URLs. It uses one connection, `BEGIN READ ONLY`, transaction-local 5-second timeout, at most three fixed manifest programs, validates and compiles each program, rejects non-`SELECT`/`WITH` SQL or write/DDL keywords, then always rolls back. It has no translator import or invocation, so shadow translation cannot execute.
- Exact authorized execution: from an authorized production environment with `DATABASE_URL` already configured, run `F1QL_PRODUCTION_GOLDEN_ENABLED=true F1QL_PRODUCTION_GOLDEN_TARGET=production npm run golden:f1ql:production > /approved/evidence/f1ql-production-golden-$(date -u +%Y%m%dT%H%M%SZ).json`. Do not redirect stderr into that artifact. A successful artifact is one stdout JSON object with `status: "passed"`, `statement_timeout_ms: 5000`, two cases, and `matched: true` for each. Retain it outside the database, record its path, SHA-256, execution UTC time, deployed commit, and operator in the release evidence, then append those details here. `status: "failed"` is evidence of a factual mismatch and exits non-zero; investigate data freshness/mapping before any rerun. No production execution was performed for this change.
- Verification: `npm run typecheck` passed; `npm run lint` passed with 0 errors and 117 pre-existing warnings; `npm run test:f1ql` passed 167 tests in 12 files; `npm run test:api:inprocess` passed 7 tests. No production database access or deployment was performed.

### Phase 5 Cross-Reference Corpus (2026-07-21)
- Classified all 100 committed local cases without changing their evidence boundary: 41 fixture-only (15 deliberate rejections and 26 source-dependent pace cases), 59 structural production-runnable cases, and zero externally factual fixture cases. Synthetic fixture output is never presented as production truth.
- Researched official authorities with seven bounded Exa searches (three results each): $0.007 each, $0.049 total. No key, request ID, or API response metadata was committed. `docs/F1QL_CORPUS_SOURCE_EVIDENCE.md` records the method, source URLs, exact counts, and limits.
- Expanded the guarded production manifest from five to thirteen programs: three structural checks and ten cited factual checks across FIA/Formula 1 sources. The factual projection covers 1950 metadata; 2014 double final-race points; 2019 fastest-lap points; 2021 abbreviated Belgium classification; 2022 scoring interval; 2024 Bahrain classification/metadata; and 2025 standings, qualifying, and metadata. It exercises all four production sources: standings, race classification, qualifying classification, and event metadata.
- The manifest remains read-only and translator-free. It is capped at sixteen programs, skips unavailable views, and has not been run against production under this task. The 2022 sprint source was researched but is explicitly not asserted because F1QL has no sprint-session source.
- Verification: `npm run typecheck` passed; `npm run lint` passed with 0 errors and 117 pre-existing warnings; `npm run test:f1ql` passed 171 tests in 12 files. No production database access or deployment was performed.

### Phase 5 Final Season Standings Facts (2026-07-21)
- Extended the bounded production manifest with six FIA-cited final Drivers' Championship checks, each restricted to `f1ql.driver_standings` (the F1QL projection of `season_driver_standing`). The programs filter one season/driver and select the recorded standing using `MAX`; they never query race classifications or derive totals by summing race points.
- Final champion facts: 2014 Lewis Hamilton, P1, 384; 2019 Lewis Hamilton, P1, 413; 2021 Max Verstappen, P1, 395.5; 2022 Max Verstappen, P1, 454; 2024 Max Verstappen, P1, 437; 2025 Lando Norris, P1, 423. This covers 2014 double-points, 2019 fastest-lap, the 2021 sprint trial, the 2022 top-eight sprint era, 2024, and the 2025 no-fastest-lap era.
- Authorities are FIA 2014/2019 classification pages and final Abu Dhabi Championship Points documents 60 (2021), 38 (2022), 58 (2024), and 56 (2025), linked in `docs/F1QL_CORPUS_SOURCE_EVIDENCE.md`. Ten additional bounded Exa searches (three results each, 700-900 extracted characters) were used only to locate these FIA sources; no key, request ID, or response metadata was retained.
- Added a manifest invariant test proving all six final-total programs target standings and contain no `sum` measure. The former 2025 structural standings projection also now uses `MAX`, eliminating its unnecessary sum. The manifest remains capped at 24 programs (18 currently: three structural and 15 factual).
- Local verification: `npm run typecheck` passed; `npm run test:f1ql` passed 172 tests in 12 files; `npm run lint` passed with 0 errors and 117 pre-existing warnings.
- Production verification: committed `2838fab` was run with `railway run --no-local --service main --environment production` and both required golden flags. The runner returned `status: "passed"`, `statement_timeout_ms: 5000`, and all 18 cases passed (three structural, 15 factual); no view was skipped. The six final standings returned the expected PostgreSQL numeric values: Hamilton 2014 `384.00`, Hamilton 2019 `413.00`, Verstappen 2021 `395.50`, Verstappen 2022 `454.00`, Verstappen 2024 `437.00`, and Norris 2025 `423.00`, each at championship position 1. The runner used one `BEGIN READ ONLY` transaction and rolled it back; no deploy or database write occurred.

### Phase 5 Non-Winner Factual Checks (2026-07-21)
- Expanded the bounded production manifest from 18 to 26 programs: three structural and 23 FIA/Formula 1-cited factual cases. New FIA-cited 2025 checks cover final Drivers' Championship P2 Max Verstappen (421), P3 Oscar Piastri (410), and P20 Franco Colapinto (0), all restricted to `f1ql.driver_standings` with recorded `MAX` measures rather than race-point sums.
- New canonical `f1ql.event_classification` checks cover Australian GP P2 Max Verstappen (18), P3 George Russell (15), and Fernando Alonso DNF (null position, normalized `dnf`; production records null points). New canonical `f1ql.qualifying_classification` checks cover Australian GP P2 Oscar Piastri and P3 Max Verstappen; the existing cited Australian qualifying metadata check remains included. The FIA-authoritative Australia DNS and Las Vegas DSQ rows were absent from `f1ql.event_classification` on the guarded run, so they are documented coverage gaps rather than manifest assertions.
- Authorities are FIA Abu Dhabi Championship Points Document 56, FIA Australian final race and final qualifying classifications, and FIA Las Vegas final race classification, recorded in `docs/F1QL_CORPUS_SOURCE_EVIDENCE.md`. Four bounded Exa searches (three results, at most 700 extracted characters) located FIA materials; no key, request ID, or response metadata was retained.
- Local verification: `npm run typecheck` passed; `npm run lint` passed with 0 errors and 117 pre-existing warnings; `npm run test:f1ql` passed 172 tests in 12 files.
- Production verification: committed `5da0e0a` was run through `railway run --no-local --service main --environment production` with both golden flags. The runner returned `status: "passed"`, `statement_timeout_ms: 5000`, 26 passed cases (three structural and 23 factual), and no missing views. New verified values were Verstappen P2/`421.00`, Piastri P3/`410.00`, and Colapinto P20/`0.00`; Australian race Verstappen P2/`18.00`, Russell P3/`15.00`, and Alonso null position/null points/`dnf`; Australian qualifying Piastri P2 (`75180` ms) and Verstappen P3 (`75481` ms); and Australian qualifying metadata `Australian Grand Prix`, `2025-03-16`, `qualifying`. The runner used one `BEGIN READ ONLY` transaction with the five-second local timeout and rolled back; no deployment or database write occurred.

### Phase 5 Metamorphic and Differential Tests (2026-07-21)
- Added Docker-fixture metamorphic coverage for generic classification `source`, `filter`, `sort`, and `limit`: inversion is asserted only across unique non-null positions, small limits are ordered prefixes of larger limits, empty filters are no-ops, and independent filters may be reordered without changing results. Both race and qualifying classification are covered.
- Added compiler-versus-reference differential execution for fixture-backed standings aggregate/sort/limit, event and qualifying classification (including descending `NULLS LAST`), and staged lap-pace median aggregation. All compiled SQL executes only through `executeF1QLReadOnly` in the local wrapped test database.
- Fixed the reference interpreter's classification null ordering so `NULLS FIRST`/`LAST` is independent of sort direction, matching PostgreSQL. Existing PR CI already runs `npm run test:f1ql`, so the new suite is automatically covered; no production access or deployment was performed.

### Phase 5 Initial Golden Corpus (2026-07-21)
- Added `tests/fixtures/f1ql-golden-corpus.json` with 15 question/program cases: 10 fixture-runnable cases across standings, pace, race and qualifying classification, composable entity filters, and both event-metadata session scopes; five expected schema, participation, and cost-budget rejections.
- `scripts/snapshot-f1ql-golden-corpus.ts` generates `tests/fixtures/f1ql-golden-corpus-programs.json` from the real parser/lowering emitter. `tests/f1ql/golden-corpus.test.ts` compares that emitted core IR exactly, executes only explicitly runnable corpus cases through the ordinary read-only executor, and asserts typed rejections. The corpus runner does not invoke translation and therefore cannot execute shadow output.
- Verification: `npm run typecheck` passed; `npm run lint` passed with 0 errors and 117 pre-existing warnings; `npm run test:f1ql` passed 67 tests in 9 files; `npm run test:api:inprocess` passed 7 tests. No deployment was performed.

### Phase 5 Golden Corpus Expansion (2026-07-21)
- Expanded the data-driven F1QL corpus to exactly 100 cases: 85 fixture-runnable programs and 15 schema, execution, and typed-validation rejections. It covers standings aggregates/ranks/driver and season filters; pace summary and delta driver order, round, clean-air, compound, empty-result, and no-shared-event boundaries; race and qualifying classification limits, null order, status, driver, team, and combined filters; and default/race/qualifying event metadata scopes.
- Added explicit contracts for every typed validation rejection: `participation_missing` executes through the local fixture database; `definitions_version_mismatch`, `complexity_exceeded`, `coverage_unsupported`, and `signature_invalid` are invoked at the validation boundary because the schema deliberately prevents those malformed raw programs from reaching execution.
- Replaced hand-authored runnable result rows with `tests/fixtures/f1ql-golden-corpus-results.json`, generated by `scripts/snapshot-f1ql-golden-corpus-results.ts` using the ordinary local read-only executor and shared golden fixture seed. `scripts/snapshot-f1ql-golden-corpus.ts` generates the matching real-emitter core-IR snapshot. Both generators are available through wrapped npm scripts; no external credentials or production access were used.
- Verification: `npm run golden:snapshot:f1ql-corpus` passed; `npm run golden:snapshot:f1ql-corpus-results` passed; `npm run typecheck` passed; `npm run lint` passed with 0 errors and 117 pre-existing warnings; `npm run test:f1ql` passed 164 tests in 11 files; `npm run test:api:inprocess` passed 7 tests. No deployment was performed.

### Phase 1 Metrics Fixture
- Local shadow route test observed: succeeded=1, invalid=2, unavailable=1, identity_miss=1, unsupported=1.
- PARTIAL: `railway status --json` reports the production service plan as `hobby`. Retention is not exposed by the CLI and must be verified in Railway plan documentation/dashboard before asserting a window. The report uses timestamps actually returned by Railway. To complete 30-day review, configure a durable log export if the verified retention is under 30 days.
- Decision: `tests/fixtures/f1ql-shadow.log` is force-added because `*.log` is globally ignored for runtime logs; this fixture is a deterministic parser contract, not an operational log.

### Phase 1 Production Shadow Review (2026-07-19)
```text
# F1QL Shadow Translation Review
- Window: no retained events
- Attempts: 0
- Success rate: 0.00%
- Readiness: Keep shadow-only; insufficient volume or success rate.
```
- A known-driver production request returned a canonical `pace_summary` in shadow mode after the timestamped logging deployment.
- Expected caveat: legacy pre-timestamp log lines would appear as `unknown` if returned by Railway; none were returned by the headless fetch.
- PARTIAL: Railway CLI returned no structured retained events for `--since 30d`. Validate project log-retention/export configuration before relying on the automated 30-day report.
- PARTIAL: `npm run test:f1ql` could not start because the local Docker daemon was unavailable. Start Docker and rerun `npm run test:f1ql`; database-backed Phase 1 tests were green before this environment outage.

### Phase 2 Validation Pipeline (2026-07-21)
- Delivered all gates: participation checks against `season_entrant_driver`; coverage/signature enforcement against `F1QL_SIGNATURES`; configurable AST node budget; active definitions-version refresh input; and a configurable transaction-local read-only statement timeout.
- Rejections are typed through validation, shadow-route log reason, Prometheus/JSON metrics reason label, and report parser: `participation_missing`, `complexity_exceeded`, `coverage_unsupported`, `definitions_version_mismatch`, and `signature_invalid`.
- Docker-backed proof: `npm run test:f1ql` passed 39 tests, including a `pg_sleep` query cancelled by a 10ms configured timeout. `npm run test:api:inprocess` passed 7 tests after adding phase-required 2030 entrant fixtures. `npm run typecheck` passed; lint passed with 0 errors and 117 pre-existing warnings.
- Railway observability loop proven. `railway status --json` confirmed service `main`, environment `production`, Logs V2. `railway logs --service main --environment production --since 30d --json` returned historical runtime JSONL, not build/deploy-only output; each event has the documented top-level `timestamp`, `message`, and `level` envelope. A real production `POST /program/translate` returned a shadow `pace_summary`; fetching `--since 5m` contained `[F1QLTranslation]` in `message`; `npm run report:f1ql-shadow -- /var/folders/p9/gh5frnt56_l4t03p8dl_fq3m0000gn/T/opencode/railway-production-5m.jsonl` reported one attempt, 100.00% success, `validated_shadow_program: 1`, `pace_summary: 1`.
- Fix: set Express `trust proxy` to exactly `1` hop for Railway, restoring per-client rate-limit keys and eliminating `ERR_ERL_UNEXPECTED_X_FORWARDED_FOR`; successful translation reasons are excluded from report rejection ranking.
- Decision: Railway logs are deployment-scoped. The automated 30-day review therefore covers only the window since the latest deployment, not a cross-deployment rolling period. The durable-sink decision remains deferred and must not be implemented without explicit instruction.
- PARTIAL production release: dead deployment `ebc4a0e0-74cf-4ec9-8a53-76633334f320` was superseded once by `43b9103e-d953-4e09-b946-bc9ddb7fec97` after a committed green state. The replacement stayed `BUILDING` with `deploymentStopped: true` on every 30-second poll for five minutes. `railway logs 43b9103e-d953-4e09-b946-bc9ddb7fec97 --deployment --lines 100` returned no deploy logs. Investigate the stopped deployment in Railway before a single new deploy attempt; do not retry blindly.
- Production completion: the existing replacement deployment later advanced to `DEPLOYING` and then `SUCCESS`; no additional deploy was created. A known-driver `POST /program/translate` returned a shadow `pace_summary`. After 15 seconds, `railway logs --service main --environment production --since 5m --json` was fetched and reported as follows:
```text
# F1QL Shadow Translation Review

- Window: 2026-07-21T06:32:58.161Z to 2026-07-21T06:32:58.161Z
- Attempts: 1
- Success rate: 100.00%

## Outcomes
- succeeded: 1

## Rejection reasons

## Operations
- pace_summary: 1

## Readiness
- Keep shadow-only; insufficient volume or success rate.
```

### Phase 0-2 Confidence Audit (2026-07-21)
- Static audit identified two correctness gaps despite the prior green suites: participation validation omitted driver-filtered standings and event-classification programs; `/program` converted typed validation failures into generic execution failures.
- Fix: participation now derives every currently scoped driver/season pair, including standings filters and event-classification filters. `/program` now returns a typed 400 rejection code and logs the validation code. Railway report parsing now ignores non-string external `message` envelopes rather than throwing.
- New Docker-backed regressions cover driver-filtered standings participation, event-classification participation, environment-backed definitions refresh, and HTTP typed participation rejection. The metric expectation was expanded to account for that new rejection.
- Verification after the audit: `npm run typecheck` passed; `npm run lint` passed with 0 errors and 117 existing warnings; `npm run test:f1ql` passed 41 tests; `npm run test:api:inprocess` passed 7 tests; `npm run test:unit:db:docker` passed 598 tests in 36 files.
- Production confidence round-trip: deployment `0e242331-c5e6-4163-823d-1f7419ff38b6` reached `SUCCESS`. A known-driver shadow request returned `pace_summary`; after 15 seconds, the Railway JSONL report showed one attempt, 100.00% success, no rejection reasons, and operation `pace_summary`.

### Phase 3 Core IR Refactor (2026-07-21)
- Checkpoint complete: `scripts/snapshot-f1ql-lowering.ts` generates `tests/fixtures/f1ql-lowering-golden.json` from the real `parseF1QLProgram` and `lowerF1QL` emitter; `tests/f1ql/lowering-golden.test.ts` compares every schema-valid shadow-corpus program exactly. The generated snapshot covers standings aggregate, pace summary, pace delta, event classification, and the schema-valid unknown-identity program.
- Next: replace bespoke core `pace_aggregate`, `subtract`, `event_classification`, and fused `sort_limit` nodes with generic Source, Filter, Aggregate, Sort, Limit, Join, Compare, and Delta nodes. Update compiler/interpreter and preserve snapshot output only where surface semantics remain intentionally unchanged; document any intentional snapshot diff before accepting it.
- Completed first compositional change: fused `sort_limit` is now `limit → sort → aggregate`. The lowering golden was regenerated from the real emitter; this is an intentional internal snapshot shape change with identical compiled SQL, interpreter rows, and API results. `typecheck`, lint (0 errors), 42 F1QL tests, and 7 in-process API tests passed.
- Completed core source boundary: standings lowering now emits a core-owned `source` node rather than reusing the surface AST type. The snapshot JSON and all 42 F1QL tests remain unchanged. Next is to generalize the source/filter types for pace and classification before replacing their bespoke compiler/interpreter paths.
- Completed bounded classification milestone: the `event_classification` surface macro now lowers to core `source(event_classification) -> filter -> sort(finishing_position asc, nulls last) -> limit`; `CoreEventClassificationNode` and its opcode dispatch were removed. Compiler and reference interpreter consume the generic chain while preserving exact predicates, parameter ordering, selected columns, ordering, and limits. The lowering golden was regenerated from the real emitter; its classification entry intentionally changed shape only. Pace nodes were not refactored.
- Verification: `npm run typecheck` passed; `npm run lint` passed with 0 errors and 117 pre-existing warnings; `npm run test:f1ql` passed 42 tests; `npm run test:api:inprocess` passed 7 tests. The shadow translator remains non-executing; its injected-executor invariant remains covered by the F1QL suite.
- Completed pace lowering: `pace_summary` now emits `source(lap_pace) → filter(valid-lap eligibility) → aggregate(median by round) → aggregate(count/mean)`. `pace_delta` emits two equivalent per-round aggregates joined on `round`, then `compare → delta`. Removed `CorePaceAggregateNode`, `CoreSubtractNode`, and their compiler/interpreter opcode dispatch. The compiler retains the established SQL parameter order for summary and delta programs; reference interpretation retains shared-event and null behavior.
- Intentional regenerated-golden difference: pace summaries formerly serialized as `pace_aggregate`; pace deltas as `subtract` with two `pace_aggregate` children. They now serialize the explicit generic `source`, `filter`, `aggregate`, `join`, `compare`, and `delta` composition. Surface renderings, API rows, valid-lap exclusions, per-event median/cross-event mean, shared-event alignment, and shadow non-execution are unchanged.
- Completed generic classification consumption: compiler and reference interpreter now recursively apply core `source`, `filter`, `sort`, and `limit` nodes. Classification no longer uses a recovered macro topology or bespoke flattened-node dispatch; source-specific SQL projection remains at the source boundary. Pace retains its bounded specialized compilation path.
- Production completion: deployment `70346161-7bd7-45e6-8abe-32530ed13f73` reached `SUCCESS`. A known-driver shadow request returned `pace_summary`; the fetched five-minute Railway report contained one attempt, 100.00% success, no rejection reasons, and `pace_summary` as the operation.
- Added `validateCoreProgram` immediately after lowering and before compilation. It applies the Phase 2 source/field signature catalog to generic core operators, rejects unsupported classification sort fields, and permits only established internal staged pace aliases. The lowering golden is unchanged because lowering output did not change; its exact real-emitter comparison remains green.
- Verification: `npm run typecheck` passed; `npm run lint` passed with 0 errors and 117 pre-existing warnings; `npm run test:f1ql` passed 43 tests; `npm run test:api:inprocess` passed 7 tests.

### Phase 3 Generic Pace Execution Completion (2026-07-21)
- Completed the remaining pace execution conversion. The compiler now dispatches lap-pace `source`, `filter`, `aggregate`, `join`, `compare`, and `delta` nodes into the established SQL shape rather than detecting pace-summary or pace-delta aggregate topologies. The reference interpreter dispatches the same generic node operations, including generic grouping, median/mean/count measures, join alignment, comparison aliases, and delta output.
- Preserved the established SQL parameter contracts: summary `[season, driver_id, rounds, clean_air_only, compound]`; delta `[season, driver_a_id, driver_b_id, rounds, clean_air_only, compound]`. Docker-backed differential tests retain eligible-lap exclusions, median-per-round then mean behavior, shared-round alignment, no-overlap null output, and output field shapes.
- Generalized core node input types so aggregate, sort, limit, and join compose through `CorePipelineNode`. Core signature validation now validates pace source fields used by generic filters/grouping and rejects a non-median generic compare input; the new regression covers that rejection.
- The lowering emitter and serialized core shape did not change in this execution-only milestone, so `tests/fixtures/f1ql-lowering-golden.json` was intentionally not regenerated. Its existing real-emitter exact comparison passed.
- Definition of done verified locally: `npm run typecheck` passed; `npm run lint` passed with 0 errors and 117 pre-existing warnings; `npm run test:f1ql` passed 44 tests; `npm run test:api:inprocess` passed 7 tests. No deployment was performed.

### Phase 4 Area 1: Qualifying Classification (2026-07-21)
- Added the `qualifying_classification` first-class F1QL source and thin surface macro. It lowers to generic `source(qualifying_classification) -> filter -> sort(qualifying_position asc, nulls last) -> limit` core IR.
- Added the source signature and participation coverage for driver-filtered qualifying programs. The compiler and reference interpreter project canonical qualifying position, best time/session, elimination round, and normalized `classified`/`dnf`/`dns` status; all query values remain parameters and execution remains read-only with a statement timeout.
- Added forward migration `migrations/20260721_add_f1ql_qualifying_classification.sql`; it exposes normalized IDs and classification status from `qualifying_results`. Docker fixtures cover canonical-view SQL execution, ordering, filters, participation rejection, signature rejection, reference interpretation, and the real-emitter lowering golden.
- Verification: `npm run typecheck` passed; `npm run lint` passed with 0 errors and 117 pre-existing warnings; `npm run test:f1ql` passed 46 tests; `npm run test:api:inprocess` passed 7 tests. Shadow translation remains non-executing; no deployment was performed.

### Phase 4 Area 2: Event Metadata and Session Scope (2026-07-21)
- Added the `event_metadata` first-class F1QL source with typed `season`, `round`, event identity/name, circuit, date, and `session_scope` fields. Session scope is constrained to `race` or `qualifying`; omitted scope explicitly lowers to `race`.
- Added `f1ql.event_metadata` through the forward migration `migrations/20260721_add_f1ql_event_metadata.sql`, generic core source/filter lowering, signature validation, compiler projection with parameterized scope, and a reference interpreter. Date output is normalized to text for SQL/interpreter parity.
- The real-emitter corpus and lowering golden include default-race metadata. Docker-backed regressions prove canonical-view execution, default and explicit qualifying scope, parameterization, rendering, and invalid generic fields. Event metadata has no driver participation requirement; shadow translation remains non-executing.
- Verification: `npm run typecheck` passed; `npm run lint` passed with 0 errors and 117 pre-existing warnings; `npm run test:f1ql` passed 48 tests; `npm run test:api:inprocess` passed 7 tests. No deployment was performed.

### Phase 4 Area 3: Composable Entity Filters (2026-07-21)
- Event and qualifying classification now lower their event scope, classification status, driver, and team predicates as ordered generic core `filter` nodes rather than a fused source-specific predicate. Event metadata likewise composes event scope and session scope through separate generic filters.
- The core filter input is recursive for supported pipelines; compiler and reference interpreter recursively apply each source-signature-approved predicate with parameterized SQL. No source coverage was broadened, and participation validation continues to use the surface driver scope.
- The real-emitter corpus and regenerated lowering golden cover a combined event/status/driver/team qualifying filter chain. Docker-backed execution tests cover composed race driver filtering and qualifying driver/team filtering, SQL parameter ordering, and interpreter parity. Shadow translation remains non-executing.
- Verification: `npm run typecheck` passed; `npm run lint` passed with 0 errors and 117 pre-existing warnings; `npm run test:f1ql` passed 49 tests; `npm run test:api:inprocess` passed 7 tests. No deployment was performed.

### Phase 4 Area 4: Retirement-Reason Sampling (2026-07-21)
- Added `src/f1ql/retirement-reasons.ts`: an intentionally conservative canonical normalization map with `unknown` as the explicit fallback bucket. Fixture coverage includes known labels and ambiguous/non-retirement labels that must remain `unknown`.
- Added `npm run sample:retirement-reasons:production`. The sampler refuses to run unless both `RETIREMENT_REASON_SAMPLING_ENABLED=true` and `RETIREMENT_REASON_SAMPLING_TARGET=production` are set, refuses local hosts, uses one connection and one bounded aggregate query (`LIMIT 100`), runs inside `BEGIN READ ONLY` with a 5-second local statement timeout, emits JSON to stdout only, and performs no persistent writes.
- PARTIAL: production sampling is intentionally unrun under this task's production-access constraint. To finish the evidence collection, run `RETIREMENT_REASON_SAMPLING_ENABLED=true RETIREMENT_REASON_SAMPLING_TARGET=production npm run sample:retirement-reasons:production` from an authorized production environment, retain the stdout artifact outside the database, and review any `unknown` labels before extending the map.
- Production sampling authorized and completed on 2026-07-21 with the guarded command. It returned the bounded 100-label aggregate through a read-only transaction. Known high-frequency categories normalized correctly; ambiguous labels (for example `Spun off`, `Collision damage`, `Clutch`, and `Turbo`) remain explicitly `unknown` pending editorial mapping rather than being guessed. No database writes or persistent output were made.
- Pace correctness migration first production attempt rolled back cleanly because PostgreSQL forbids `CREATE OR REPLACE VIEW` from changing the established `lap_time_seconds numeric` typmod to `numeric(8,3)`. Fixed the migration view projection to cast the v2 value back to untyped `numeric`, preserving the existing canonical-view contract before retry.
- Second retry also rolled back cleanly on the existing `session_type varchar(5)` view contract; the v2 projection now preserves that type explicitly before the final retry.
- Final corrected migration attempt reached production but was rejected with `cannot execute CREATE TABLE in a read-only transaction`. Railway production has only `DATABASE_URL`, with no separate primary/replica variables, and that role is read-only. No DDL was applied. PARTIAL: apply `migrations/20260721_pace_correctness_v2.sql` through the Supabase SQL editor or an approved primary migration credential, then verify `laps_normalized_v2`, `f1ql.lap_pace`, and `f1ql.event_classification` before deploying pace correctness code.
- Supabase SQL Editor retry exposed one additional legacy view contract: `f1ql.event_classification.status_reason` is `varchar(100)`. The migration now casts the revised expression to `varchar(100)`; rerun the full transaction after pulling commit containing this fix.
- Pace hardening deployment: after Railway region `us-east4-eqdc4a` was removed in the dashboard and the Supabase Session Pooler configuration was corrected, deployment `187da761-b9e8-4568-9464-8f9fda030b72` reached `SUCCESS`. A production shadow request returned `pace_summary`; the five-minute log report recorded one attempt, 100.00% success, and no rejection reasons.

### Phase 4 Pace V2 Immutable Audit Reconciliation (2026-07-21)
- Added reviewed forward-only migration `20260724_pace_v2_audit_reconciliation.sql`. It creates append-only `pace_v2_round_audit_reconciliation` evidence with one row per race session and an immutable update/delete rejection trigger. The original `pace_v2_round_audit` is never modified.
- Added a dual-flagged, loopback-refusing read-only manifest generator and explicit primary-only serializable reconciler. Both accept only `manifest_fact_fingerprint_only_v1`: race session, positive complete row count, and active methodology must exactly match the original audit and current facts, while only the original/current fact fingerprint differs. The reconciler verifies its evidence relation and trigger and does not update facts or original audits.
- Preflight now reports reconciliation relation/trigger diagnostics and accepts immutable reconciliation evidence only for that exact otherwise-valid manifest-audit mismatch. It refuses absent, altered, non-immutable, count/method/session, manifest-fingerprint, or current/original fingerprint-invalid evidence; reconciliation cannot cover another manifest defect.
- Production intentionally not run. After applying the reviewed migration through an approved primary channel, use an authorized read-only production role to generate and independently review one manifest for each affected round; then use an approved primary role to insert evidence one round at a time. Retain manifests and stdout SHA-256 evidence and rerun guarded preflight. No migration, reconciliation, production query, or deployment was performed for this change.
- Verification: `npm run typecheck` passed; `npm run lint` passed with 0 errors and 120 pre-existing warnings; `npm run test:f1ql` passed 204 tests in 21 files.

## 2026-07-18: Shadow F1QL Translation
- Decision: `/program/translate` is independently feature-gated by `F1QL_TRANSLATION_ENABLED`.
- Decision: the initial route is shadow-only and returns a validated program without calling `executeF1QL`.
- Decision: driver identities use the strict database-backed `DriverResolver`; no humanized or guessed fallback IDs are allowed.
- Decision: translation accepts only the constrained F1QL schema. Legacy intents and SQL fallbacks are prohibited.
- Fix: Anthropic translation now uses forced tool use (`emit_f1ql_program`) instead of prompt-only text JSON after a production shadow request returned non-JSON text.
- Decision: use Groq `openai/gpt-oss-20b` through the OpenAI-compatible adapter for low-cost shadow translation.
